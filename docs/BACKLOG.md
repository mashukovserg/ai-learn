# AI-Learn Backlog

> **Active roadmap:** [`ROADMAP_3M.md`](ROADMAP_3M.md) (2026-05-16 → 2026-08-15) — agents pick work from there first; this file keeps the detailed punch lists and the work log.

## Completed (2026-08-23 session — by Claude Code)
- [x] **Fork 5 closed — the `/rooms` catalog ships variant B, the "quiet card"** (owner's pick from `docs/room-card-picker.html`). Removed from `src/app/[lang]/rooms/page.tsx`: the 160px gradient cover with its radial overlays, the `%` ring (and the "0%" it shouted on every untouched room), the corner category chip, `getCoverTone`/`progressTone`, and the `hover:-translate-y-1` + `hover:shadow-xl` + cyan hover border. The card is now a 34px category-tinted icon tile + mono uppercase category, 15px title, 2-line description and a mono meta row — difficulty dot · time on the left, state on the right (`N/M` solved in cyan, `✓ completed` in accent, nothing when not started) — with a 2px progress bar on the bottom edge that renders only above 0%. Completed rooms keep the accent border and tint; hover changes the border and nothing else. The whole card became the `<Link>` (the footer arrow is gone, so nothing else would have been clickable). Supporting changes: `getCategoryTone()` maps all 8 categories to one hue each (info / cyan / orange / slate / violet / yellow / pink, accent deliberately excluded — it means "interactive/completed" per Fork 3), `difficultyDot()` colors the meta dot (Beginner accent, Intermediate warning, Advanced rose — rose, not `danger-*`: a level is a category, not an error), the progress snapshot now carries `completed`/`total` so the meta row can show solved tasks instead of a percentage, and `--color-violet-400` was added to the `[data-theme="saas"]` block (only `violet-200` had an override, so the Ideas tile would have been pale on white). Verified in the browser at RU + EN × dark + light × not-started/in-progress/completed, and at 375px (single column, no horizontal overflow, bar visible); measured card height 187–208px against ~340px before; the only console errors are the pre-existing `/api/me` 500s from the backend not running. Docs synced: `DESIGN_FORKS.md` + `.ru` (Fork 5 resolved, gradient card demoted to alternative A with restore instructions, C and D kept as options), `README.md` + `.ru`, `PROGRESS.md` + `.ru`. `check-all` green (2339 tests). (by Claude Code)
- [x] **Unpushed work recovered.** `xcode-select` turned out to be healthy again (Command Line Tools present), so the git block from the 2026-08-22 session is gone. `main` was already level with `origin/main` and the backlog edit was already committed; the only branch missing from the remote was `room/opencode-terminal-agent` (`98bb03d`, the new OpenCode room) — now pushed with an upstream set. The stale worktree `.claude/worktrees/recursing-gauss-742bd1` was clean and its commit already lives on `origin/claude/recursing-gauss-742bd1`, so it was removed. (by Claude Code)

## Completed (2026-08-22 session — by Claude Code)
- [x] **`ai-agents` enriched with academic sources and substantive text** (owner request). All 8 chapters now cite primary literature: 19 arXiv papers (ReAct, CoT, Reflexion, Toolformer, Gorilla, agent survey, AI Agents That Matter, multiagent debate, AutoGen, MetaGPT, Generative Agents, OSWorld, WebArena, RAG, MemGPT, Mem0, indirect prompt injection, Levels of AGI, METR task-horizon) + Ng's The Batch letter + Anthropic's Building Effective Agents and Computer Use posts + OWASP LLM Top 10. Every arXiv ID verified against the arXiv API in one batch (titles matched), web links fetched and confirmed; a closing bilingual "Sources" card lists all 22 with one-line annotations (frontier-evals house pattern). **Fabricated stat removed:** the ReAct "40% → 85-90%" numbers appear nowhere in the paper — replaced with its real results (+34 pp ALFWorld, +10 pp WebShop, hallucination reduction vs pure CoT), and task 5's answer/explanation updated in sync so the room stays solvable in both locales. Ng's concept claim grounded with his actual HumanEval numbers (48.1% zero-shot → up to 95.1% in an agent loop). New substantive paragraphs in every chapter, incl. tool-plumbing best practices (answering the owner's "how hard is this to set up?" — validation at the boundary, machine-readable errors, retry caps, idempotency, human confirmation), the OSWorld human-vs-model gap (72% vs ~12-22%), MemGPT/Mem0 memory engineering, least-privilege injection defense, and METR's task-horizon metric. Chapter depth after the pass: every chapter ≥240 words per language (RU 241–319, EN 258–376, DOM-measured).
- [x] **Rendering bug fixed in `ai-agents`:** `<Term id="...">` markup sat inside string literals in the ch. 2 heading and ch. 2/6/7 paragraphs and rendered as literal text on prod (screenshot from owner). Converted to real JSX (`Term` imported, `lang` passed); heading Terms dropped in favor of plain text + in-paragraph tooltips. Repo-wide sweep confirmed no other file has literal-Term strings (all other `<Term` usages are real JSX with `lang={lang}`).
- [x] **De-pathos pass over `ai-agents` per owner feedback** («слишком пафосно — давай избегать подобных суждений»): «Мы перешли от взаимодействия…», «исполнительный разум», ch. 1 title «ИИ обретает волю», «иллюзия непрерывного сознания», «высшая форма интеллекта», closing "most important skill today" slogans — all replaced with concrete, sourced phrasing; the two «это не просто» constructions in this file eliminated in the same rewrite.
- [x] **Chapter-heading (`h2`) size outliers normalized in 5 more rooms** — the answer to "did you check the other rooms for the same problems": a full h2 sweep of all 49 theories found five files mixing sizes. AiImageCreation, PostChatgptHistory, ResearchAiEra (first-chapter h2 was `text-3xl` in an otherwise all-`text-2xl` file), ChatgptMoment (one 2xl among 3xl), LlmMechanics (`text-2xl md:text-3xl` outlier among plain 3xl) — each normalized to its file's majority; all five now uniform in-file. Cross-room variance (2xl-rooms vs 3xl-rooms) noted as a possible future design-fork topic. Small-size (`text-sm`) paragraphs audited too — remaining uses are consistent card-caption roles, not body-flow mismatches.
- [x] **`check-all` protected from agent worktrees:** background-task worktrees under `.claude/worktrees/` carry their own `.next` build output, which eslint traversed — 669 foreign errors failed `check-all` mid-session. Added `.claude/worktrees/**` to `eslint.config.mjs` global ignores.
- [x] **Theory body font-size normalized across 13 rooms** (owner reported mismatched paragraph sizes in `ai-agents` chapter 1). Some chapter openers carried `text-lg` (18px) while adjacent body paragraphs were base 16px — an inconsistent "lede" applied to only some chapters in some rooms. Removed `text-lg` from all 27 plain body paragraphs in AiAgents, AiAlignment, AiImageCreation, AiRag, AiResearch, AiSecurity, ChatgptMoment, Embeddings101, FineTuning101, Llama318b, LocalModels101, NativeMultimodality, TaxonomyMatching theories. Deliberate exceptions kept: Prompting101's accent-bordered pull-quote, AiResearch's centered outro quote, AiCareerTrajectories' own internally consistent `text-base md:text-lg` system. Verified in the browser (both `ai-agents` ch. 1 paragraphs measure identical computed 16.8px); `check-all` green (2339 tests). (by Claude Code)

## Completed (2026-08-20 session, responsive pass — by Claude Code)
- [x] **Responsive/adaptive pass across the app** (audited at 375 / 768 / 1280 in the browser; an iframe probe measured `scrollWidth` of all 49 room pages + 13 app pages at 375px — 0 horizontal overflows after the fixes).
  - **Navbar:** on `<sm` the inline search collapsed into an unusable empty square. Replaced with a search icon that opens a full-width panel under the header (autofocus, same results list, closes on route change/outside tap/Escape); the "очков/pts" label hides under 400px; streak moved `sm:` → `md:`.
  - **Dashboard hero:** the CTA sat beside the text and crushed it on phones — now stacks (`flex-col sm:flex-row`), full-width button on mobile.
  - **SkillsMatrix (`/skills`):** was a hard two-pane flex that clipped the whole right column on phones. Role picker is now a horizontal chip row on mobile / vertical sidebar from `md`; header wraps (`flex-wrap`) so the score panel no longer overflows at tablet widths; paddings scale down.
  - **RadarChart:** long start/end-anchored axis labels were clipped by `overflow-hidden` ancestors on narrow viewports — viewBox now carries ±60px side padding.
  - **Room page:** task panel's internal `max-h + overflow-y-auto` scroll now applies only at `lg:` (no nested scroll trap on mobile); breadcrumb truncates long room titles and hides the category segment on `<sm`; **new floating "Задания N/M" pill** (below `lg`) scrolls to the task panel and hides via IntersectionObserver while the panel is on screen — tasks used to sit ~10–22k px below the theory with no way to reach them.
  - **Theory headings guard (globals.css):** chapter `h2/h3/h4` are `flex` and a `<Term>` splits the text into non-wrapping flex items — `chatgpt-moment` (543px), `ai-security` (499px), `ai-alignment` (407px) overflowed 375px viewports. One rule (`.reading-prose h2/h3/h4 { flex-wrap: wrap }`) fixed all rooms at once, current and future.
  - **TaskCategorize:** bucket labels/chips now `break-words` (long uppercase words overflowed the 3-column bucket grid).
  - **Task i18n parity fixes (bilingual gate):** `TaskQuestion` (Check/Done, Submit Answer, Show/Hide Hint, "Select all that apply", incorrect-message, "Correct!") and `TaskSorting` (Drag to reorder, Check Sequence) showed hardcoded English on RU pages — all now switch on `useLang()`.
  - `check-all` green (lint + tsc + 2339 tests). Desktop regression checked at 1280: inline search, sticky task sidebar with internal scroll, 2-col room grid, pill hidden.
- [x] **Fork 5 opened — room card design** (owner dislikes the current gradient-cover cards). Built `docs/room-card-picker.html` in the house picker pattern: current card (A) faithfully replicated next to three alternatives — B "quiet flat card" (recommended; ~180px vs ~340px tall, passes anti-Vibecode + ROADMAP_VIEW_MODE gates), C "category spine", D "terminal ls-list" (as a future grid/list toggle, not a replacement) — each rendered on real room content in three progress states with a dark/light stage toggle. Registered as Fork 5 in `DESIGN_FORKS.md` + `.ru` with trade-offs and an implementation note (`rooms/page.tsx`, cyan hover → accent). Decision pending owner review. (by Claude Code)

## Completed (2026-08-20 session — by Claude Code)
- [x] **`frontier-evals-logic` rebuilt — the worst room of the 50-room audit (score 95) closed.** The old theory was ~263 EN / 235 RU words across 4 unnumbered stub sections styled outside the house card markup, and at least 4 of 13 tasks referenced theory that did not exist (Wang's "reasoning paradigm", "Compute Gap", "Data Sovereignty", saturation stages). Full rewrite to 6 chapters + labeled short block + Sources, every chapter 277–498 words / 4–5 paragraphs per language, measured in the rendered DOM.
  - **Every claim fact-checked against primary sources before writing** (5-agent workflow, WebFetch on lastexam.ai, the HLE Nature paper, arXiv, ARC Prize, Epoch AI, TechCrunch, transcribed Wang interviews). Corrections folded in: GSM1k contamination drop is up to 8 p.p. in the final NeurIPS version (not the preprint's 13%); o3's high-compute ARC-AGI run cost thousands of dollars PER TASK, not in total; the "day to think" claim grounded with AlphaProof's up-to-three-days at IMO 2024; and Wang does NOT use the literal terms "Compute Gap"/"Data Sovereignty" — his framing is three pillars (compute/algorithms/data), gigawatt curves, "data dominance", "strategic asset", "national AI data reserve" — so the theory attributes the substance to Wang and the labels to the discourse, and tasks 4/11 were reworded to drop the false attribution.
  - **Task repairs:** task 5's broken "All of the above" design (whose correct answer included the joke option "humans got tired of checking") replaced with a single defensible contamination answer + three clearly false distractors; tasks 4 and 11 reworded as above. All 13 tasks now trace to specific theory sentences in both locales — including the previously unsupported saturation stages (ch. 1), North Star (ch. 4), compute gap and data exhaustion (ch. 5), and chain-vs-answer grading (ch. 3).
  - **Two real exhibits from lastexam.ai (captured 2026-08-20):** the official AI-progress chart (Nov 2024 single digits → Jul 2026 fifty-plus percent — the benchmark life cycle in time-lapse, placed in the HLE chapter) and the Quantitative Results table with BOTH columns, Accuracy and Calibration Error (GPT-4o: 2.7% accuracy at 89% calibration error — the overconfidence lesson, placed in the leaderboard-reading chapter). Assets under `public/images/rooms/frontier-evals-logic/`. Solvability guard checked: neither capture reveals a task answer.
  - 2 new glossary entries (`benchmark-contamination`, `test-time-compute`); room time 25m→35m; the Grok 3 cons@64 chart dispute (Feb 2025) told neutrally as the fair-configuration lesson feeding task 13. `check-all` green (2339); both locales verified in the rendered DOM (8 sections, 2 figures, 0 term-not-found, 0 literal entities).
- [x] **`docs/REFERENCES.md` created** — the owner's collection of outside projects that inspire AI-Learn; first entry: AI Interpretability School (ai-interpretability.school) with take/skip analysis. Registered in the CLAUDE.md docs index; the derived room ideas (interpretability cluster: `llm-interpretability`, `agent-trace-reading`) logged in `ROOMS_IDEAS.md` + `.ru`. (by Claude Code)

## Content queue — underdeveloped rooms (audit of all 50, 2026-08-18, by Claude Code)

A 14-agent audit measured every room against the chapter depth gate (>=240 words / >=4 paragraphs per language) and the visual-variety gate, then verified exhibit URLs. 30 of 50 rooms have zero visual exhibits. Ranked queue (top of the debt; `ai-literature-reviews` and `research-ai-era` from the original ranking were fixed the same day):

1. [x] ~~**`frontier-evals-logic` (95)**~~ — rebuilt 2026-08-20, see the entry above. The ENTIRE theory is ~263 EN / 235 RU words across 4 unnumbered sections — less than the gate demands of ONE chapter. Worse, at least 4 of 13 tasks reference theory that does not exist (grep finds no "reasoning paradigm", "Compute Gap", "Data Sovereignty", or benchmark-saturation stages anywhere in the theory) — a live solvability violation. 9 of 13 tasks are multiple-choice. Exhibit ready to capture: https://lastexam.ai/ leaderboard cropped to model names + accuracy column (frontier models in single/low-double digits) — headroom = not yet saturated; alternative: https://epoch.ai/benchmarks FrontierMath panel.
2. [ ] **`ai-regulation-eu` (93) — thinnest theory per chapter.** All five chapters run 62–91 words against the 240 bar, 1–2 paragraphs plus a bullet list each. Also a factual discrepancy to verify against the OJ text: Chapter 5 says "€7.5M or 1.5% of turnover" for supplying incorrect information, while Article 99(5) of Regulation (EU) 2024/1689 sets 1%. Exhibit: EUR-Lex consolidated text, Article 99 'Penalties' §3–5 (the three fine tiers) — https://eur-lex.europa.eu/eli/reg/2024/1689/oj ; primary source, settles the discrepancy. Solvability guard: the fine-amounts input task must be rechecked if the crop shows the figures.
3. [ ] **`prompt-evals` (92) — 100-line theory file.** Two numbered chapters plus an unheaded card grid; Ch2 is 74 EN / 70 RU words. The 9-task layer is the only healthy part. Exhibit: promptfoo web viewer eval matrix (rows = test cases, columns = variants, green/red cells, per-column pass rate) — capture from a local `npx promptfoo view` run, fully genuine; login-free alternative: https://lmarena.ai/leaderboard cropped to score + 95% CI columns.
4. [ ] **`ai-regulation-ru` (91) — same shape as the EU twin.** Five chapters at 61–109 words, 2 paragraphs each; the RU locale is consistently the SHORTER side even though the subject is Russian law; the draft-law claims carry no checkable citation. Exhibit: the official Кодекс этики ИИ registry at https://ethics.a-ai.ru/ — signatories count + first register rows, in Chapter 1 next to the AI-Alliance self-regulation paragraph.
5. [ ] **`post-chatgpt-history` (90).** 4/4 chapters under the gate, zero visuals. Exhibit: Hugging Face model card header for deepseek-ai/DeepSeek-R1 (MIT license chip, downloads counter, model tree finetunes/quantizations) — the open-weights argument made visible.
6. [ ] **`research-grounding` (90).** 4/4 chapters under the gate, zero visuals despite being the RAG/citations room. Exhibit: a Perplexity answer with inline citation superscripts + Sources rail — behind bot verification, owner must capture manually (same blocker logged for `search-retrieval-to-synthesis` ch. 2).
7. [ ] **`agentic-swarm-management` (90), `llm-guardrails` (90), `ai-image-creation` (90).** All chapters under gate, zero visuals. Exhibits verified: Scale SEAL leaderboard table; Llama-Guard-3-8B model card S1–S13 hazard-taxonomy table (chapter names LlamaGuard but never shows a policy); SD 3.5 Space advanced-settings panel (negative prompt, seed, CFG, steps — the chapter's own vocabulary as UI).
8. [ ] **Task-mix violations:** `ai-security` and `native-multimodality` are MCQ/input-only — each needs at least one `sorting` or `mentor` task (authoring gate).

Method note: depth measured on rendered prose per language; scores from the audit's composite (chapters failing, visuals, task health). Full per-room JSON preserved in the session transcript.

## Completed (2026-08-18 session — by Claude Code)
- [x] **Visual exhibits added to both recovered rooms (closes the follow-up logged the same session).** Both were content-heavy rooms with zero screenshots.
  - **`research-ai-era` ch. 4** — a capture of the Semantic Scholar page for Vaswani et al. (2017), cropped to the title block and the tab row where `189 200 Citations` and `41 References` sit side by side. It is the exhibit the chapter needed: both directions of a citation chain shown as ordinary links on one real page. Full sandwich — intro naming what the reader will see, `<Screenshot>` frame with bilingual alt + caption, then two interpretive paragraphs on why the asymmetry (finite references backward, unreadable volume forward) dictates different tactics, and on the limits (a counter measures attention, not quality; the citation-type breakdown is automatic and errs; index coverage is incomplete). Solvability checked: task 7 is an `input` whose answer is "citation chaining" — the phrase appears nowhere in the capture.
  - **`ai-literature-review`, active-learning chapter** — the ASReview LAB review screen from the tool's own documentation (asreview.readthedocs.io → Screening): one record card with title, abstract, keywords and the `Relevant` / `Not relevant` buttons. The interpretive paragraph below reads what the interface deliberately omits — no confidence score, no explanation, no queue position — and why: showing the prediction would make the human agree with it and destroy the independence of the label the model trains on. Solvability checked: task 13 is a `timeline` over the five steps of the active-learning cycle, none of which the capture reveals.
  - Both captured headless at 2× device scale and cropped; the Semantic Scholar crop deliberately excludes the region a cookie banner covered, so nothing was dismissed or clicked. Assets: `public/images/rooms/research-ai-era/semantic-scholar-citations-references.png`, `public/images/rooms/ai-literature-review/asreview-lab-screening-screen.png`. Verified in a full-page headless render of both rooms in both locales: exhibit framed, captioned and interpreted; assets serve 200 raw and through the Next image optimizer.
  - **Fixed while verifying: HTML entities written inside plain JS string literals rendered literally.** `&apos;`, `&laquo;`, `&raquo;` and `&quot;` only decode inside JSX text; in a `{ru ? '…' : '…'}` string React escapes them, so the EN page of `ai-literature-review` was showing 39 literal `&laquo;`/`&apos;` sequences to learners (most of them pre-existing, from the room's original chapters) and `research-ai-era` 2. Replaced with real `«»`/`’` characters — 48 occurrences across the two files, 0 literal entities in the rendered HTML afterwards in both locales. A repo-wide scan found no other affected room: elsewhere the entities sit inside multi-line JSX fragments, where they decode correctly. (by Claude Code)
- [x] **Two finished-but-unreachable rooms recovered; room count 48→49.** A project-state review found `research-ai-era` and `ai-literature-reviews` on disk — full bilingual theory plus 10 tasks each — registered in **none** of `ROOMS_METADATA`, `ROOM_TASKS`, or `THEORY_COMPONENTS`. `git log -S` confirms they were never registered: added 2026-07-21, unreachable ever since, while `docs/ROOM_DEVELOPMENT.md:106` listed them as shipped 2026-07-11. The existing registry guard could not see them because it compares two in-memory maps and never reads the directory. Owner decided: wire `research-ai-era` as a room on the `agentic-systems` path, and merge `ai-literature-reviews` into the live `ai-literature-review` as chapters (its id differed from the live room by a single `s`).
  - **`research-ai-era` (Intermediate, 35m, `agentic-systems`, right after `deep-search-agents`)** — Abbott's method translated to working with agents: puzzle vs topic, the history of bottlenecks, controlled vocabulary before the query, three search modes + citation chaining, the five source questions applied to an agent's answer, the researcher as orchestrator. All 7 chapters were below the depth gate (RU never exceeded 210 of 240 words; Ch3 and Ch6 were bare 2-paragraph stubs); thickened to **382–655 RU / 453–757 EN words, 4–6 paragraphs each**, measured in the rendered DOM. Also removed a one-off decorative underline on the intro pull-quote (anti-vibecode gate) and normalised five categorical heading hues (`text-info-400`/`purple`/`rose`/`warning`/`cyan`) to `text-heading`.
  - **`ai-literature-review` absorbed the SLR-tooling chapters (35m→60m, 10→20 tasks, 6→13 sections).** New chapters, each written up to the depth gate: what a systematic review is and why it costs a year; where the AI actually lives (active learning, and the blind-spot risk of a one-sided seed set); the tool landscape (19 of 21 tools automate screening, only 4 extraction; SVM and bag-of-words still dominate; only 4 of 21 open their code); the LLM generation and why it does not replace a protocol; three challenges (intelligence, usability, evaluation) plus why screening is measured with F2/WSS rather than F1; trust, transparency and human-in-the-loop. Donor tasks renumbered 11–20 (ids now 1–20, no gaps); `Bolaños et al. 2024` added to the Sources block; the stale "as Chapter 4 teaches" cross-reference de-numbered.
  - **6 missing glossary entries added** (`empirical-puzzle`, `controlled-vocabulary`, `prepared-serendipity`, `citation-chaining`, `slr`, `active-learning`). Without them `Term.tsx` would have rendered six red "[Term not found]" strings in both locales — verified 0 occurrences after.
  - **New guard: `src/data/rooms/__tests__/no-orphan-content.test.ts` (5 tests)** reads the filesystem instead of the maps — every task file must have a `ROOMS_METADATA` entry and a `ROOM_TASKS` wiring, every `*Theory.tsx` must be imported by the registry, and all three registries must agree on the same id set. This is the test that would have caught the orphans a month ago.
  - **Cover-image defect fixed repo-wide.** 8 rooms declared `image:` paths that 404 (`/images/ai-alignment.png`, `/images/ai-rag.png`, `/images/ai-security.png`, `/images/ai-research.png`, `/images/research-grounding.png`), rendering a broken-image box on the room page. `image` is now optional in `LocalizedRoomMetadata` (the page already guarded on it), the dangling paths were removed, and the data-integrity test was strengthened from "non-empty string" to "declared ⇒ the file exists under `public/`".
  - **Catalog icon bug fixed.** 7 icon names used in metadata (`BookOpen`, `Library`, `Landmark`, `Layers`, `Network`, `Flame`, `HardDrive`) were missing from the room catalog's `ICON_MAP` and silently fell back to `Bot`; imported and mapped.
  - Verified on the running dev server in both locales: `research-ai-era` 7 chapters / `ai-literature-review` 13 sections, 0 `[Term not found]`, 0 empty paragraphs, both rooms present in the catalog and on the `agentic-systems` path. `check-all` green — **2339 tests** (was 2247). The `TaskTimeline` hydration warning seen during verification reproduces on untouched rooms (e.g. `agentic-cli-tools`) and is the pre-existing defect already logged below. (by Claude Code)
- [x] ~~ **Follow-up — no visual exhibit in `research-ai-era` or in the merged `ai-literature-review`.** Both are content-heavy rooms with zero screenshots, against the visual-variety gate. Candidates identified but not captured (the capture agents were cut short by a session limit): the ASReview LAB screening view (open source, self-hostable, shows the human include/exclude decision and the reprioritisation counter — belongs in the active-learning chapter), and a Semantic Scholar citation-graph view for the citation-chaining chapter in `research-ai-era`. Neither was shipped rather than shipping filler. (logged by Claude Code)
- [x] **Solvability of all 20 `ai-literature-review` tasks re-verified end to end (closes the follow-up below); one gap fixed.** Each donor task (11–20) was checked fact-by-fact against the merged theory in BOTH locales: the six-stage order (task 11) is stated verbatim in the "What a systematic review is" chapter; 19-of-21-screening / 4-extraction (task 12) in the tool-landscape chapter; the five active-learning steps (task 13) in the screening chapter; all six tool names of task 14 are classified in the landscape and LLM chapters; asymmetric error costs + sampled checks of the rejected pile (task 15), F2/WSS (task 16), the three swap-SVM-for-LLM obstacles (task 17), RAG grounding (task 18), the first inclusion criterion (task 19), and every scenario outcome of task 20 (ASReview outside biomedicine, Covidence/RCT, Excel majority, chat-has-no-protocol) all trace to specific sentences. AI-Act options in tasks 16/17 are pure distractors — nothing in the theory supports them, which is what a wrong option needs. Original tasks 1–10 were re-checked for contradictions with the new chapters (none; task 3's ordering is compatible with the six-stage list). One real gap found and fixed: the "11 LLM tools" count cited by task 18's explanation had been dropped in the merge — restored to the LLM chapter in both locales. `check-all` green (2339). (by Claude Code)
- [x] ~~**Follow-up — solvability of the 20 merged `ai-literature-review` tasks was not re-verified end to end.** The donor's 10 tasks were written against the donor's chapters, which now sit in a different room with different neighbours; shapes and ID sequencing pass the Vitest gate, but the "answerable from this room's theory in both locales" walk-through was cut short by a session limit. (logged by Claude Code)

## Completed (2026-08-10 session — by Claude Code)
- [x] **New room `search-retrieval-to-synthesis` (Intermediate, ideas-history path, right after `chatgpt-moment`)** — built from the owner-supplied source set (Shah & Bender 2022/2024, Bender et al. 2021, Liu et al. 2023, Sharma et al. 2024, Shah & West 2024, Zhou & Li 2024). Placement decided against embedding into an existing room: `research-grounding` (20m, practical), `deep-search-agents` (agent mechanics) and `ai-rag` (retrieval internals) all cover the technology, none can carry a media/epistemology argument about the institution of search; cross-links added to all four in "Where to Go Next". 6 bilingual chapters, each ≥4 paragraphs per the depth gate: the ranked list as a division of labour (and its by-products — spread, provenance, serendipity, literacy as a side effect); the generative pipeline and what changes when the unit of output stops being a document; the Shah & Bender diagnosis written in March 2022, eight months *before* ChatGPT, resting on Stochastic Parrots; the measurements (51.5% of sentences fully supported / 74.5% of citations supporting, fluency inversely correlated with verifiability; the CHI 2024 echo-chamber effect; why users actually switch); the economics of sources as three feedback loops; and a verification protocol plus the cases where synthesis does not fit. 11 tasks across 7 types (timeline, categorize, sorting, mentor, scenario, MCQ, multi-select, input) — incl. a mentor task against "Perplexity shows citations, so provenance is solved" and a contested-brief scenario where the winning move treats the answer as an index. 5 new glossary terms (`generative-search`, `provenance`, `verifiability`, `serendipity`, `echo-chamber`). One real visual exhibit: a Wikimedia Statistics capture of English Wikipedia page views split by agent type, sandwiched with the honest caveats (the Spider/Automated handover is a reclassification artifact; the chart shows the mechanism, it does not prove causation). Perplexity and Google AI Overview captures were **not** taken — both sit behind bot detection, which the assistant will not bypass; logged below as a follow-up for the owner to capture manually. New room-level Vitest guard `src/data/rooms/__tests__/search-retrieval-to-synthesis.test.ts` (8 tests) pinning registry wiring, path position, task mix and `<Term>`-to-glossary integrity. Room count 47→48; gate green (2247). Verified in both locales on the running dev server. (by Claude Code)
- [ ] **Follow-up — missing screenshot exhibits for `search-retrieval-to-synthesis` ch. 2.** The chapter describes three product shapes (Google AI Overviews, Perplexity, ChatGPT Search) in prose. Genuine captures of an AI Overview block above the organic links, and of a Perplexity answer with citation chips, would make the "one answer instead of a list" claim visible. Both products are behind Cloudflare bot verification for automated sessions, so these must be captured manually by the owner and dropped into `public/images/rooms/search-retrieval-to-synthesis/`. (logged by Claude Code)

## Completed (2026-08-10 session — by Claude Code)
- [x] **Frontend structural refactor — locale contract + room-page decomposition (no content text touched).** Scope chosen by the owner from four candidate areas. (1) **Locale contract centralized** in the new `src/types/lang.ts` (`Lang`, `LANGS`, `DEFAULT_LANG`, `isLang`, `toLang`): the pair `'en' | 'ru'` had been re-declared ad hoc in two files and recovered from `string` by ~40 unchecked `as 'en' | 'ru'` / `as 'ru' | 'en'` casts across 13 files — a bad `[lang]` segment produced `undefined` LocalizedString lookups with no type error. Locale is now narrowed once at each boundary (route params in the dashboard, rooms catalog, room page, both paths pages, professions, settings; the cookie in `proxy.ts`; `LangProvider`) and passed down as `Lang`; every cast outside the theory components is gone, `useLang()` returns `Lang`, `getDictionary` and `resolveTask` take `Lang`, and `generateStaticParams` + the settings language picker are driven off `LANGS` instead of hardcoded pairs. The theory registry stays `{ lang: string }` on purpose (documented inline: `ComponentType` unions in `ComponentClass`, whose `defaultProps` makes props invariant), so all 50 theory components were left untouched. (2) **`rooms/[id]/page.tsx` decomposed 355 → 188 lines** into three units with one job each: `src/components/TaskRenderer.tsx` replaces a six-branch ternary chain that repeated the same five props per branch (adding a task type is now a `case`, not a page edit); `src/hooks/useRoomTasks.ts` owns locale resolution, completion state, persistence, the reset nonce and the success-modal trigger; `src/lib/taskDoneSound.ts` holds the Web Audio chime (module-level context reuse instead of a per-page ref). The chime moved out of the state updater onto a solved-ids ref, so a double-invoked updater in StrictMode can no longer double-fire it. (3) **New Vitest suite** `src/__tests__/lang.test.ts` (10 tests) pins the fallback behaviour that replaced the casts. `check-all` green (2247 tests, +10). Verified on the owner's dev server in both locales: room theory + task list render, solving a task moves progress 0%→7%, reset returns to 0% and clears `progress:llm-mechanics` from localStorage, rooms catalog/paths/settings render, settings language picker reads the current locale. Pre-existing and unrelated: a hydration mismatch in `llm-mechanics` theory (a non-deterministic token count — reproduced on the unmodified baseline via `git stash`) and `/api/*` 500s (backend not running locally). (by Claude Code)

## Completed (2026-08-02 session — by Claude Code)
- [x] **New room `ai-alignment-limits` (Intermediate, ideas-history path)** — built from the owner-supplied Schuster & Kilov 2025 paper (AI & Society, "Moral disagreement and the limits of AI value alignment"). Placement decided against alternatives: the technical `ai-alignment` room covers HOW RLHF/CAI work, this room covers WHY their outputs may lack justification — mechanism vs justification, cross-linked both ways; the argument-anatomy genre continues `ai-existential-risk`, giving the ideas-history path a coherent finale (singularity → x-risk → alignment limits). 5 bilingual chapters (each ≥4 paragraphs per the depth gate): reliability≠safety and the two kinds of reasons (epistemic/political); Delphi & Moral Machine and where the speech-recognition analogy breaks (no independent fact for aggregation to converge on); the dual challenge as a premise chain incl. the builders' own retreat to the political line; RLHF and constitutional AI under the same test (CAI's transparency gain honestly credited, presupposed-not-justified charged); what follows and what does not (narrow-class scope, creeping technocracy, the practitioner's question) with the paper's two-track programme. 12 tasks across 6 types — incl. a mentor task against "RLHF learns from people so it's democratic" and a moderation-feature scenario where the winning move narrows autonomy + adds justification and appeal. New `value-alignment` glossary term. DOI cited. Room count 46→47; gate green (2178, +102). Verified in both locales via the owner's running dev server. (by Claude Code)

## Completed (2026-08-02 session — by Claude Code)
- [x] **`chatgpt-moment` deepened (owner review): Ch5 rewrite + agents springboard, Ch2 thickened with a second screenshot exhibit, risks block broadened, Task 10 replaced.** Owner flagged Ch5 ("Почему это именно Момент") as too thin (~90 words / 2 paragraphs) and wanted a springboard to the agents room. (1) **Ch5 rewritten** into 6 paragraphs (≥240 words / ≥4 paras per language) around a real thesis — a "moment" is an *access event*, not a capability event (GPT-3 existed since 2020; only access changed on 2022-11-30); spectacle-vs-moment (Deep Blue/AlphaGo happened *to* a champion, not *to you*); the Mosaic/Netscape analogue (interface, not invention, made the web a moment); Search→Generation as the shared root of every prior-chapter disruption; a **springboard paragraph linking to the `ai-agents` room** (chatbot → "model with hands"); and an honest anti-hype caveat (the curve underneath was smooth). (2) **Ch2 (RLHF) thickened** with a concrete base-vs-aligned example paragraph and a "cost of alignment / sycophancy" paragraph, plus a **second product-screenshot exhibit** per the proactive-screenshots rule — a genuine capture of the InstructGPT paper page on arXiv (2203.02155, 4 Mar 2022), captured headless (Playwright @2×, cropped to title→submission-history), rendered via the `<Screenshot>` component with the full sandwich (intro above, bilingual alt+caption, interpretation below) — reinforces "obedience > size" (1.3B InstructGPT preferred to 175B GPT-3) and that the moment's foundation predates it by 8 months. (3) **Risks block broadened** from 2 thin paragraphs to 3 — hallucinations (tied to the Ch2 sycophancy point), the copyright/lawsuit backlash (NYT v. OpenAI, data provenance), and the labor-anxiety / "turning point vs bubble" open question. (4) **Task 10 replaced** — the old sorting task (Netflix/Twitter/Instagram time-to-1M) was unsolvable from theory (those timings appear nowhere); new sorting task orders the precursors Transformer (2017) → GPT-3 (2020) → InstructGPT (RLHF) → ChatGPT, fully derivable from Ch1–2 and reinforcing the Ch5 "capability matured for years" thesis. `check-all` green (2123); verified in both locales via SSR markers + a headless render of the exhibit-in-context; image serves 200 raw and via the Next optimizer. New asset: `public/images/rooms/chatgpt-moment/instructgpt-arxiv.png`. (by Claude Code)

## Completed (2026-07-31 session — by Claude Code)
- [x] **New room `ai-literature-review` — "Literature Review in the AI Era" (43 rooms total), two-way cross-linked with `deep-search-agents`.** Closes the owner-requested pipeline item (queued 2026-07-24). Framing: AI collapses the *find* half of a literature review to minutes but adds a signature failure mode — hallucinated references (fluent, perfectly formatted, nonexistent) — so the reviewer's job shifts from finding to judging and verifying. Theory: 5 chapters in the plain-prose style, each ≥240 words / ≥4 paragraphs per language (measured RU 249–274 / EN 285–320) — (1) what a review is (narrative vs systematic), why an agent changes it (opens with a link to `deep-search-agents`), the hallucinated-citation risk, reproducibility anchor; (2) question → protocol: PICO framing, sub-query decomposition, inclusion/exclusion criteria written *before* searching, source triage; (3) systematic screening + deduplication, two-pass (title/abstract → full text), a PRISMA-style flow in a `<pre>` funnel diagram, recording exclusion reasons + dual screening; (4) verifying citations: the ghost-reference failure, the resolve-DOI → confirm-venue → confirm-authors → confirm-quote sequence, red flags, retractions/versions, a verification-status workflow, `<MiniReferenceWindow>` citation rule; (5) synthesis vs summary, evidence tables, guarding against over-claiming (echoes deep-search quality gates), the audit trail, surfacing gaps, a closing link back to `deep-search-agents`. 10 tasks (MCQ ×3, multiple-select ×2, sorting, categorize, input, scenario, mentor) — every one answerable strictly from theory; sorting + mentor both present per the task-mix rule. Reverse cross-link added to `DeepSearchAgentsTheory` ("Next: …" pointer). Registered in metadata (Practice, Intermediate, 35m, `BookOpen` icon — added to `roomVisuals` ICON_MAP), tasks index, lazy theory registry, and the `agentic-systems` path (right after `deep-search-agents`). `check-all` green (1970 — data-integrity + task-shape suites picked the room up automatically); forbidden-phrase scan clean. Verified in browser on `/ru` + `/en`. (by Claude Code)

## Completed (2026-07-24 session — by Claude Code)
- [x] **Clickable screenshots with a full-screen lightbox.** Product-screenshot exhibits were an inline, non-clickable `next/image` in an accent frame — unreadable up close, especially on mobile. Extracted the pattern into a first-class `src/components/Screenshot.tsx`: the framed thumbnail is now a button (zoom-in cursor + hover magnifier badge) that opens a full-screen lightbox (dark backdrop, centered natural-size image, pinch-zoomable on mobile), closable via ✕ / backdrop click / Escape (with body-scroll lock). Converted all three existing exhibits to it (`ai-history`, `chatgpt-moment`, `llama-3-1-8b`), added "Tap to view larger" to the bilingual captions, and updated the `docs/AGENTS.md` "Product screenshots" gate to mandate the component over hand-rolled markup. `check-all` green (1923); lightbox open/close verified on a 390px touch viewport. (by Claude Code)

## Completed (2026-07-29 session — by Claude Code)
- [x] **`gh` (GitHub CLI) added to AC-206 chapter 5 — the missing link between "the agent made a patch" and "the agent sees the result" (owner's request).** A coverage check found zero mentions of `gh` anywhere in the platform: git is taught well (`git-safety-net`, `agentic-cli-tools`), CI is taught as YAML, but everything living on the GitHub side — opening a PR, reading check status, pulling the failing log — was browser-only. Added to chapter 5 of `github-actions-ci`, right after the paragraph about giving an agent material rather than the word "failed": an intro paragraph (what `gh` is, install + `gh auth login`), a `<Terminal>` walking a red run (`gh pr checks` → per-check statuses with one failing → `gh run view --log-failed` → the assertion line → `gh run download --name playwright-report`), a paragraph mapping the three commands to the three questions plus `gh run watch` for scripting, and a boundary paragraph: the client acts as the signed-in account, `gh pr merge` hits branch protection exactly like the web button, fork PRs still need maintainer approval — so an agent keeps the reading commands (`checks`, `view`, `download`, `watch`) while merging and re-runs stay with a human. Chapter 5 is now 8 paragraphs / 541 ru / 674 en words. New glossary entry `gh-cli` wired as `<Term>`; inline commands use `<code>` per the room's existing convention. Two new tasks (11 input `gh pr checks`, 12 multiple-select on what `gh` does and does not grant), both answerable strictly from the new text; room task count 10→12. Verified in browser on `/ru` + `/en`: terminal renders, tooltip resolves, no "[Term not found]", no glued `<code>` spans (checked by regex over the rendered text). CURRICULUM (en+ru) task count updated. `check-all` green (2076). (by Claude Code)
- [x] **`llm-mechanics` repair pass: locale parity, chapter numbering, depth, and four unsolvable tasks.** A coverage review of the transformer-fundamentals topics (attention, transformers, tokens, positional embeddings, context windows, KV cache, inference) found them all concentrated in this one room and surfaced five defects the owner asked to close. (1) **Locale parity in Chapter 1** — RU and EN were offset by one paragraph: the opening analogy existed only in RU, the Russian-tokenizer cost note only in EN. Both locales now carry all five paragraphs. (2) **Chapter numbering** — the file had two "Chapter 3" (Self-Attention, Temperature) and two "Chapter 4" (Positional Encoding, Context Window) plus stale `Chapter 5: Temperature` comments; renumbered 1–9 sequentially. (3) **Chapter 7 (hallucinations)** shipped an empty `<p>` in the EN branch and an EN-only bullet list; rewritten as four/five parallel paragraphs in both locales. (4) **Depth gate** — every prose chapter (1–7) now clears ≥4 paragraphs and ≥240 words per language; measured live in the DOM: RU 279/281/271/301/388/264/245, EN 318/334/323/335/438/293/324. Ch. 8 (checklist) and Ch. 9 (lab) are intentionally concise and now carry the explicit `Краткий блок` / `Short block` label the gate requires. (5) `CURRICULUM.md` + `.ru.md` still described the room as "Tokens, Context Windows, Next-Token Prediction, Temperature" with 4 (EN) / 8 (RU) tasks — corrected to the real 14 tasks and the real topic list. **The added prose was chosen to close solvability holes, not to pad length:** tasks 6 (top-p/nucleus), 11 (Q, K, V matrices + sinusoidal labels), 12 (Greedy → Beam → sampling ordering), 13 (repetition penalty) and 14 (context-window growth) referenced concepts no chapter taught in either language. Chapter 3 gained the Q/K/V mechanics and normalized significance weights, Chapter 4 the sinusoidal encoding and RoPE (`<Term id="rope">`), Chapter 5 a decoding-strategies paragraph (greedy / beam / top-k / top-p / repetition penalty), Chapter 6 the window-growth trajectory (GPT-2 ~1k → GPT-3 ~2k → Claude 2 100k → Gemini 1.5 1M+) and the KV-cache-vs-VRAM limit. **One task edited:** task 4's correct answer included "accumulation of computational noise with each new token" — a claim no chapter makes and one that does not hold up; replaced with the KV-cache/VRAM limit (now taught in both locales) and the noise option rewritten into a distractor. Also removed the leading heading icons (`Info`, `BrainCircuit`, `Layers`, `Zap`, `Thermometer`) per the no-icons-in-headings gate, and moved the "Куда дальше" CTA from the middle of Chapter 1 to the end of the room. Verified in browser on `/ru` + `/en`: 9 sequential chapters, `h2 svg` count 0, no `[Term not found]`, 10 glossary tooltips, both Short-block labels, zero empty paragraphs, task 4 rendering the new options; no console errors. `check-all` green (2076). **Not done here:** the room still has no visual exhibit (screenshot gate) — logged as follow-up. (by Claude Code)
- [x] **Two new rooms close the git/GitHub gap: `git-safety-net` (AC-105) and `github-actions-ci` (AC-206).** A content-gap review of all 43 rooms showed git and GitHub existed only as decoration: real coverage was the brand-new AC-205 (branch protection, required checks, PR review), one branch-per-task chapter in AC-104, and passing mentions in AC-201 — while AC-202/AC-205 already assume the learner can read a diff and roll back. The owner picked the top two gaps from the proposal list. **AC-105 (Beginner, placed right after AC-102, before AC-103):** git framed as insurance rather than bookkeeping — the working folder is a draft, not storage; the diff as the unit of review (read the file map first, then the three signal files — check config, tests, dependency files — and removed lines more carefully than added ones); the commit as the unit of undo with the staging area used to split one agent batch into separately revertable commits; undo chosen by where the change currently lives (restore/stash → local commit reset → shared-branch revert), with a reflog rescue after a hard reset; branch + worktree per task for parallel agents, plus the pre-flight checklist. 4 terminal exhibits (`git diff --stat` with an unexpected `.github/workflows/ci.yml` line, batch splitting via `add -p`, reflog rescue, `worktree add/list`). **AC-206 (Intermediate, placed right before AC-205):** the clean-machine argument (an agent's “all green” describes the environment it shaped itself); workflow anatomy — file in `.github/workflows`, triggers → jobs → steps, with a real minimal `ci.yml`, the forgotten-checkout trap and action pinning by SHA; speed and cost — cache keyed on the lockfile, parallel jobs, matrix, path filters, and the boundary where saving cancels the check; secrets vs run-token permissions, least privilege, the “agent does not edit workflows” rule and reduced permissions for fork PRs; artifacts, branch protection as what makes a check required, the three causes of red CI and why the re-run button only fits the third. 3 terminal exhibits (`cat ci.yml`, a parallel run list with a matrix failure and a skipped docs-only build). Both rooms: 5 bilingual chapters each, every chapter ≥4 paragraphs and ≥240 words per locale (measured: 263–469); 10 tasks each across 6 types (multiple-choice, input, multiple-select, sorting, categorize, mentor, scenario), IDs 1–10, all answerable from the room's own theory in both locales; registered in `THEORY_COMPONENTS`, `ROOM_TASKS`, `ROOMS_METADATA` and the `agent-coding` path; `GitBranch` + `Workflow` added to the room-catalog `ICON_MAP`. 12 new bilingual glossary terms approved by the owner before shipping (`diff`, `commit`, `staging-area`, `revert`, `reflog`, `worktree`, `ci-cd`, `workflow`, `runner`, `matrix-build`, `build-artifact`, `ci-secrets`). Verified in browser: both rooms render in `/ru` and `/en` (5 chapters each, terminals and `<Term>` tooltips present), a task submit was exercised end-to-end (explanation shown, progress 10%). `check-all` green (2068). Note: these are additional to the `ROADMAP_3M.md` M2-1 list (AC-301…AC-402), which stays open. (by Claude Code)
- [x] **New room AC-205 `agentic-release-control` — the release layer the agent-coding path was missing (43 rooms total).** Audit prompted by the AC-102 screenshot work: no room covered CI itself, required checks, PR review of agent output, canary/flags, or rollback in prod — the topic existed only as passing mentions in AC-101 (retry/fallback/rollback chain), AC-102 (release-control tool group), AC-201 (one quality-gate paragraph + rollback checklist) and AC-202 (which stops at the green local loop). `prompt-evals` covers eval-based release gates for LLM features — a deliberately separate boundary. Theory: 5 chapters in the plain-prose style — (1) local run vs shared CI (clean environment, matrix, "check passed" ≠ "change allowed") with a local-vs-CI `<Terminal>`; (2) quality gate as independent blocking conditions + `branch-protection`, and the three separate permissions (propose / start checks / merge) evidenced by a real "Action required — awaiting approval from a maintainer" run; (3) review of an agent PR: the size requirement, reading order (file list → code-to-tests ratio → migrations/permissions/secrets/dependencies → style last), agent review as a first pass with its blind spot; (4) canary vs feature flag and how to read pre-set thresholds on request rate / error ratio / P50-P95-P99; (5) rollback ladder (flag → traffic → revert), migrations caveat, blameless review, and the autonomy budget. 11 tasks (MCQ ×5, input, sorting, multiple-select, categorize, mentor, scenario), all answerable strictly from the theory. Three real screenshots captured headless (Playwright, dark, 2x, contributor handles blurred) into `public/images/rooms/agentic-release-control/`: GitHub Actions run awaiting maintainer approval, the PR Files-changed view (+41 −1, two files, one a new test), and a `play.grafana.org` service dashboard with the 5% error-ratio threshold line — each in the full sandwich (intro, accent-border `next/image`, bilingual alt/figcaption, interpretive paragraph). New glossary entry `branch-protection`; `quality-gate` / `canary-release` / `feature-flag` reused from the same-day AC-102 work. Registered in metadata (Agent Coding, Intermediate, 35m, icon Rocket), tasks index, lazy theory registry, and the `agent-coding` path right after AC-202. README / PROGRESS / AGENTS room inventory / CURRICULUM (en+ru) updated to 43 rooms. Verified in browser on `/ru` + `/en`: 5 chapters, all three exhibits load through the Next image optimizer, no "[Term not found]", tasks render, room shows up in the catalog and on the path page; no non-API HTTP failures. `check-all` green (1974). (by Claude Code)
- [x] **`agentic-coding-tools` chapters 3–5 thickened to the depth bar + two new terminal exhibits (owner's request).** The owner pointed at chapters 3, 4 and 5 of AC-102; all three were pre-2026-07-23 two-paragraph stubs. Now 5 paragraphs per locale each (~450 ru / ~430 en words per chapter). Ch3 (tool contracts): the contract reframed as "what you'd write down for a new teammate before giving repo access", schema as error-space reduction (enum/range/required), a new `<Terminal>` "agent · tool contract" showing a rejected call (`unknown argument "verbose" · allowed: path, pattern, timeout_s`) followed by the corrected one, then the interpretive paragraph — rejection text is input for the next step; two failure classes (malformed call → reformulate vs environment refusal → stop and escalate); idempotency (a retry must not apply the change twice); exit code 0 ≠ correct result (filtered tests, linter missing a new file, patch in an adjacent code path) → acceptance criteria, tests/static analysis/`evals`; trace + operational limits. Ch4 (security): the data-vs-order rule stated up front (anything obtained through a tool is data), mundane injection examples (README "run this script", ticket "delete the old config branch"), a new `<Terminal>` "agent · policy" showing a blocked instruction found inside file content and escalation to a human, then least privilege in three levels (read inside the working copy / write to the working branch, no force-push, no CI-config edits / deploy behind `human-in-the-loop`), `sandbox` for execution and secrets kept out of context, guardrails firing pre-execution and visibly, observability closing the loop. Ch5 (operations): cost mechanics made concrete (each tool result re-enters the `context-window`, so step ten carries the history of nine), the predictable leaks (re-reads, 2000-line logs, fix-break-fix loops) and their cures, a new `<Terminal>` "agent · run summary" (14/20 calls, stop after two attempts without improvement, handover + saved trace), transient vs persistent tool failure with `circuit-breaker`, staged rollout gated on metrics rather than the release calendar, rollback as routine containment. Terms reused from the existing glossary only (`human-in-the-loop`, `sandbox`, `circuit-breaker`, `context-window`, `evals`, `quality-gate`, `prompt-injection`, `guardrails`) — no new entries. Solvability re-checked against tasks 3, 5, 7, 9, 10: the theory now states the contract traits, the safe-handling rule, the tool-onboarding order and the cost lever explicitly, and no exhibit spells out an `input` task's answer. Verified in browser on `/ru` and `/en` (full chapter text renders, both terminals render, no console errors); `check-all` green (1923). (by Claude Code)
- [x] **`agentic-coding-tools` chapter 2 — CI screenshot exhibit + three release-control glossary terms (owner's request).** The owner pointed at chapter 2 ("Tool Map for a Coding Agent") and asked for a visual and tooltips on the English release-control terms. Added glossary entries `quality-gate`, `canary-release`, `feature-flag` (bilingual; a gate is a set of independent blocking checks, canary is traffic-share rollout with metric comparison, a flag is a config switch that makes rollback cheap) and wired all three as `<Term>` in the tool-group sentence — these terms already appear in the room's categorize and scenario tasks, so the tooltips also close a solvability gap. New exhibit `public/images/rooms/agentic-coding-tools/github-actions-quality-gates.png`: a real GitHub Actions run page for a `build_and_test.yml` pull-request run in the public `vercel/next.js` repo (Playwright headless, dark theme, deviceScaleFactor 2, PR author's handle and avatar blurred), embedded with the full sandwich — intro paragraph naming what the reader will see, `next/image` in the accent-border frame, bilingual alt + figcaption, interpretive paragraph below (green check = passed, grey = skipped by trigger rules; dozens of independent jobs, each able to block the merge; the agent may propose a patch and start checks but may not declare the result acceptable). Chapter 2 was a pre-2026-07-23 two-paragraph chapter, so it was brought up to the depth bar in the same task: now 5 paragraphs per locale (~350 ru / ~330 en words), the fifth splitting the release-control group by the question each tool answers (gate = may this ship, canary = who tries it first, flag = how to switch it off without shipping again). Verified in browser on `/ru` and `/en`: exhibit framed and captioned, `canary` tooltip renders, image serves 200 through the Next optimizer; the only console errors are the pre-existing `/api/me` + `/api/progress` 500s from the backend not running (guest mode). `check-all` green (1923). (by Claude Code)

## Completed (2026-07-27 session — by Claude Code)
- [x] **Screenshot rule strengthened from reactive to proactive (owner's rule).** The owner set a standing convention: use screenshots **everywhere suitable** to keep theory pages diversified and varied — not only when a chapter happens to discuss a GUI product. Amended `docs/AGENTS.md` → "Product screenshots" (renamed "…use them" → "…seek them out"): added a **variety mandate** bullet (aim for ≥1 genuine visual exhibit per content-heavy room; the `chatgpt-moment` Wikimedia Pageviews chart is the neutral-instrument model; other suitable spots — model catalogs, benchmark leaderboards, Google Trends, primary-source docs) and a **suitability-over-quota guardrail** bullet (genuine + load-bearing + sandwiched; if you can't capture a real one, log the gap in BACKLOG rather than shipping filler — a quota never justifies a fake/decorative image). All existing mechanics (authenticity, sandwich, accent-border framing, bilingual alt/caption, solvability) unchanged. Added a matching one-line **Screenshots / visual variety gate** to the CLAUDE.md Mandatory Authoring Gates digest. Docs-only change — no runtime surface. (by Claude Code)

## Completed (2026-07-23 session — by Claude Code)
- [x] **Chapter depth bar raised to ≥4 paragraphs (owner's rule) + AC-202 thickened to meet it.** The owner flagged `agentic-testing-loop` as too thin — measured: all 5 chapters sat at exactly 2 paragraphs. A fleet sweep showed this is the norm, not the exception (roughly three quarters of existing chapters are below 4 paragraphs), so the gate update is scoped honestly: the Chapter Text Depth Gate in `AGENTS.md` now requires **≥240 words / ≥4 body paragraphs per language** for **new chapters and any chapter substantively edited**, while pre-existing thin chapters are recorded as standing debt to be thickened when a room is touched — with an explicit warning that padding prose to length is worse than short prose. CLAUDE.md digest updated. AC-202 itself expanded now: +2 substantive bilingual paragraphs per chapter (10 new paragraph pairs) — test engineering requirements (determinism/speed/isolation, the ten-minute-suite trap), vague-criterion self-grading, why Red→Act order matters for agents specifically (spec vs mirror), short-stride practice with the chapter-5 brake, triage signal quality (error format over error fact) and the four triage outcomes with actions, where flakiness comes from and why agents lack default skepticism, quarantine/re-run/determinization tactics, the escalation package (report, not cry for help), and limits as experiment budget. Verified in both locales via SSR markers. Gate green (1871). (by Claude Code)
- [x] **Screenshot pilot shipped — first three product-screenshot exhibits per the new AGENTS.md rule.** Captured via headless Chromium (Playwright, deviceScaleFactor 2) into `public/images/rooms/<room-id>/` and embedded with the full sandwich pattern (intro above, `next/image` in an accent-border frame, bilingual alt + figcaption, interpretive paragraph below): (1) `ai-history` — the 1955 Dartmouth proposal page from the Stanford McCarthy archive (the "2 month, 10 man study" first line, four signatures), placed between the Dartmouth card and the birthplace-myth subblock with a link to the primary source; (2) `chatgpt-moment` — Wikimedia Pageviews chart of the "ChatGPT" Wikipedia article (flat zero before 2022-11-30 → hundreds of thousands/day, 54M first-year views), placed after the "low-key research preview" insight card (planned Google Trends capture was rate-limited 429 in headless; Pageviews is the better neutral-instrument exhibit anyway); (3) `llama-3-1-8b` — the llama3.1 entry of the Ollama catalog (117.6M downloads, 8b/70b/405b tags), placed right after the Ollama terminal as its GUI pair. All three captures verified by eye before embedding (real pages, no personal data). Verified in browser: exhibits framed + captioned + interpreted in both locales; all three assets serve 200 raw and via the Next image optimizer. `check-all` green (1923). (by Claude Code)
- [x] **New room `local-rag-docs` — the first concrete-task local-models room (42 rooms total).** Owner asked for rooms that close CONCRETE tasks with local models; chosen task: private semantic search over one's own document archive (NDA files, interviews, drafts) — the case where local is mandated by the task, not ideology, and a natural synthesis of `local-models-101` + `embeddings-101` + the multilingual-embeddings gap discussed on 2026-07-22. Theory: 5 chapters in the plain-prose style — (1) the concrete task and why cloud is forbidden; (2) RAG in plain terms, index vs query phases, the 6-step chain; (3) model choice: the language trap (English-only MiniLM silently fails on mixed RU/EN corpora → multilingual-e5/bge-m3, MTEB link), generator by hardware/language, the same-model iron rule; (4) build session in `<Terminal>` (ollama pull bge-m3 + qwen2.5:7b → index.py → ask.py with sourced answer) + sources-as-trust; (5) retrieval-first debugging and honest local-vs-cloud limits. 11 tasks (MCQ ×5, sorting, input, categorize, multiple-select, mentor, scenario), each answerable strictly from theory. Glossary wired via 6 existing `<Term>` ids (rag, chunking, embeddings, vector-db, quantization, vram) — no new glossary entries. Registered in metadata (Open Models, Intermediate, 35m), tasks index, lazy theory registry, and both paths (`beginner` after embeddings-101, `intermediate` at end). CURRICULUM updated (en+ru). Current design tokens used (text-heading, accent ramp). Verified in browser on `/ru` + `/en`: title, all 5 chapters, terminal, MTEB link, 6 glossary tooltips, tasks render; no "[Term not found]". `check-all` green (1923 — suites picked the room up automatically). (by Claude Code)
- [x] **Design tokens phase 2: status palettes → semantic roles** (`red→danger`, `amber→warning`, `blue→info`, `green→success`). Measured first: 305 literal uses across 41 files. Ramps added to `@theme` with values **copied verbatim from Tailwind v4's default theme** (oklch strings from `node_modules/tailwindcss/theme.css`) so the migration is a visual no-op; the existing `[data-theme="saas"]` literal overrides moved shade-for-shade onto the new roles. Mechanical sweep left 0 literals of the four palettes outside `globals.css`. Guard updated: the four palettes moved to `RETIRED_PALETTES` (+4 checks, gate now 1871). **Deliberate scope cut:** categorical content hues (cyan/rose/purple/violet/pink/orange/slate/yellow, ~110 uses) are NOT status colors — forcing them into `info` would be semantically false; they stay literal and tracked. Verified: light theme — probe classes equal old hex exactly (`#dc2626`/`#d97706`/`#2563eb`/`#16a34a`); dark theme — utility→token wiring proven exact via same-variable comparison (an earlier "mismatch" was a lab/oklch serialization artifact, and one probe used a class that never existed in code). Production build green. AGENTS.md role table extended. (by Claude Code)
- [x] **`ai-history`: review-defect cleanup (5 fixes; code landed inside `2c64969`).** (1) Task 10 solvability closed — Chapter 4 now states the LSTM is an improved RNN with "memory cells" holding context longer than a plain RNN, so the Logic Rules → RNN → LSTM → Transformer ordering derives from theory; (2) task 7 timeline no longer self-spoiling — years removed from event labels («Дартмут 1956» → «Дартмутский семинар» etc.; `TaskTimeline` reveals the `year` field only after completion); (3) EN/RU parity restored in "The Pattern" block — the Deep Blue commentator quote added to RU; (4) full-paragraph italics removed from the Minsky/Rosenblatt closing paragraph (Anti-Vibecode gate); (5) leading `Cpu`/`Zap` icons removed from the two chapter subheadings (+ unused imports dropped). Verified in browser on `/ru` + `/en`: new LSTM sentence and RU quote render, no `svg` inside any `h4`, new timeline labels present, old year-labels gone. `check-all` green (1867). Note: these edits were swept into the structural-refactor commit `2c64969` by a concurrent session — logged here for traceability. (by Claude Code)
- [x] **Structural refactor: lazy theory registry, extracted task resolver, one dynamic path page, vitest cleanup.** Measured first: the room page was 494 lines, of which ~294 were 35 static theory imports + a hand-maintained THEORY_COMPONENTS map — meaning every visitor of any room downloaded the code of ALL 41 theories in one client bundle, and adding a room meant editing two places. The four learning-path pages were copies differing by 12 lines. Changes: (1) `src/components/theory/index.ts` — THEORY_COMPONENTS rebuilt on `next/dynamic`: each room ships only its own theory chunk, SSR preserved (verified: chapter text present in server HTML); a new registry guard (`registry.test.ts`) makes a missing mapping fail `check-all` instead of silently rendering the old "coming soon" placeholder, and also catches orphan entries. (2) The 59-line inline locale resolver was extracted from the room page's `useState` initializer into `src/data/rooms/resolveTask.ts` — pure, typed, unit-testable; the page now just adds completion state. Room page: 494 → 355 lines. (3) The four per-path pages were replaced by one `paths/[pathId]/page.tsx` with `generateStaticParams`; the two hardcoded intros moved into a new optional `PathMetadata.intro` (falling back to `description`); locked (`intermediate`) and unknown paths 404, preserving old behaviour. Verified: all four paths 200 with intros and ordering intact, locked/unknown 404. (4) vitest: the deprecated `vite-tsconfig-paths` plugin replaced with a plain resolve alias — no more warning on every run. Deliberately NOT touched: the 5 old-style two-tree theory components (content risk, no structural gain) and the status-color palettes (tracked as phase 2 by the design-token guard). Gate green (1867, +2 registry checks); production build green, path pages statically generated. (by Claude Code)

## Completed (2026-07-22 session — by Claude Code)
- [x] **New room `ai-existential-risk` (Intermediate, ideas-history path)** — built from two owner-supplied sources: Bostrom, "Existential Risk Prevention as Global Priority" (Global Policy 2013) and Katja Grace, "Counterarguments to the basic AI x-risk case" (AI Impacts 2022). Checked for overlap first: `ai-singularity` covers the idea and its camps, `ai-alignment` covers technical methods, and Bostrom appeared in the project in exactly one sentence — the typology, maxipok and the counterarguments were absent everywhere. **Angle chosen: argument analysis, not a verdict** — the room teaches decomposing a claim about the future into premises and locating where a disagreement actually sits, which is a transferable skill and lets both sources be represented honestly. 5 bilingual chapters: existential risk defined by irreversibility rather than casualties + Bostrom's four classes (extinction / permanent stagnation / flawed realisation / subsequent ruination) with his own concession that the last two are evaluative; the expected-value priority argument and maxipok, contrasted with maximin (the worst case is always catastrophic, so maximin paralyses choice); Grace's three premises (goal-directedness → misaligned goals → control) and the chain property that an argument is no stronger than its weakest link; counterarguments sorted by the premise they attack; and the "proves too much" corporations test, plus Grace's own caveat that the objections weaken the case without establishing safety. 12 tasks across 6 types incl. a mentor task refuting "counterarguments prove AI is safe" and a scenario about locating the crux. Both sources cited with DOI/URL. Gate green (1865, +55). Verified in both locales; path order places it after `ai-singularity`. (by Claude Code)

## Completed (2026-07-21 session — by Claude Code)
- [x] **Path room order made an explicit curriculum decision (+ fixed a cross-path "next room" bug).** The owner flagged that `context-engineering-101` sat at the very end of the agent-coding path. Root cause was structural, not a one-room slip: `getRoomsByPath` returned `ROOMS_METADATA.filter(...)`, so path order was **array position** — a record of when each room was added, not a teaching sequence. Symptoms found: agent-coding had AC-201 at position 3 (Module B wedged into Module A) and AC-204 last, after the Advanced Claude Code room; the beginner path put the **Advanced** `ai-research` at position 5, between Beginner and Intermediate rooms. A plain array reshuffle could not fix it — `mcp-tool-ecosystems` belongs to two paths and needs a different position in each, which one array index cannot express. **Order now belongs to the path**: added `roomIds: string[]` to `PathMetadata` and declared a deliberate sequence for all 5 paths (agent-coding: Module A → how the loop actually runs (Claude Code loop, CLI, context engineering) → Module B quality loops → advanced tail; beginner: concepts → hands-on → applied → Advanced last; ideas-history: chronological with scaling before singularity). Membership stays on the room (`pathIds`), and rooms missing from `roomIds` are appended rather than hidden, so a wiring slip degrades gracefully. **Bug fixed along the way:** the completion modal computed "next room" as `ROOMS_METADATA[index + 1]`, so finishing the last room of a path handed the learner an unrelated room from another path — replaced with a path-aware `getNextRoomInPath()` that returns null at the end of a path. Guards added (both proven by deliberate regression): `roomIds` must exactly match the rooms declaring that path (no missing, no extras, no duplicates), and every room must point at its in-path successor with the last pointing nowhere. `check-all` green (1810, +16 checks). Verified in browser across all 4 path pages. (by Claude Code)
- [x] **Production caught up + Vercel connected to Git (auto-deploy now live).** Production was serving a 9-day-old build with the new room 404ing; all 7 deploys in the project's 154-day history had been manual `vercel --prod` runs. (1) **Merge:** the branch did *not* fast-forward as an earlier check suggested — `origin/main` had moved ahead with PRs #3–#6 (including PR #5 carrying earlier work from this branch, and the GROK task pack). Histories had diverged; force-pushing the local `main` would have destroyed four merged PRs. Reset local `main` to `origin/main` and did a real merge instead: the only conflicts were in `BACKLOG.md`/`.ru.md` where both sides appended session sections — resolved by keeping **both**, then fixed section ordering to newest-first (07-21 had landed below 07-18). Gate on the merged tree: **1794 tests** (1772 mine + 22 from the GROK pack), design-token guard clean — the incoming content introduced no literal palettes. Production build verified before pushing. (2) **Deploy:** `vercel --prod` from `main`; verified live — `context-engineering-101` renders with 12 task cards, IBM Plex Sans, `accent-500 #10b981`, gray headings, aubergine terminal, theme toggle. (3) **Git connection:** `vercel git connect` failed repeatedly with `You need admin or write access` even after the GitHub App was installed and scoped to `ai-learn`. Root cause: the App grants the *app* access, while Vercel checks write permission using the GitHub identity linked to the **Vercel account** — re-authorizing GitHub under Vercel → Account Settings → Sign-in Methods fixed it instantly. Documented the whole topology (and this exact trap) in `DEPLOYMENT.md`. Pushes to `main` now deploy automatically. (by Claude Code)
- [x] **Architecture: design changes made cheap — semantic accent ramp + a Vitest guard.** The owner asked that the architecture anticipate design changes instead of charging a repo-wide sweep for each one. Measured the real cost surface first: terminal/fonts/surfaces were already token-driven (minutes), but the brand accent was hardcoded as `emerald-*` in **662 places** and heading recolor had just cost 250 replacements. Root cause: the project had a **primitives** layer but no **semantic role** layer — `--color-term-*` and `--color-heading` were the only exceptions, and they were exactly the cheap ones. Added an accent ramp (`--color-accent-100…950`) in `@theme` + its `[data-theme="saas"]` override, mirroring the emerald shade scale so alpha modifiers (`bg-accent-500/10`) keep working and the migration was a **visual no-op**; then a mechanical 1:1 sweep `emerald-N → accent-N` across 66 files / 656 replacements (globals.css primitives untouched). **Swapping the whole site accent is now 7 lines** — verified live by overriding the ramp to Sage at runtime: nav, breadcrumbs, chips, callouts and progress all followed. Guard: `src/__tests__/design-tokens.test.ts` (in `check-all`) fails on any literal `emerald-*` in components and on any theory heading painted with the accent (encoding Fork 3), naming offending files; it also tracks the not-yet-migrated status palettes so the debt is visible. **The guard earned its keep on day one** — it caught 21 headings across 9 files that the earlier Fork-3 regex missed (they used `-500`/`-300`, not `-400`), all fixed; and a deliberate regression test proved it fails and recovers correctly. Documented as a mandatory role-not-palette rule in `AGENTS.md` (with the raster-assets boundary: PNG covers do not follow the accent) and as Fork 4 in DESIGN_FORKS(+ru) with the Sage candidate paste-ready. `check-all` green (1772). (by Claude Code)
- [x] **Skills `context-engineer` + `ai-native` designed and shipped** (`.claude/skills/*/SKILL.md`, EN per repo convention). Homework from the owner's course (lessons 2–4), but written original and adapted to this repo rather than copying the course listing — the course assumes a `memory/MEMORY.md` layout this project does not have. `context-engineer` maps the real layer system (CLAUDE.md constitution → AGENTS.md gates → BACKLOG/PROGRESS/CURRICULUM/ROOM_DEVELOPMENT/DESIGN_FORKS → topic files), states "ground truth wins over docs" (counts from `metadata.ts`, tests from Vitest), and its AUDIT checklist encodes the failure modes this repo actually hit: counter drift across 6+ files, RU-mirror lag, staleness vs `git log`, forbidden phrases, **orphaned untracked docs**, broken cross-links, and **DESIGN_FORKS drifting from `globals.css`**. Operations: audit / add (with a layer decision tree) / update / restructure / cleanup. `ai-native` covers create-skill, create-agent (minimal tools, explicit write zones, research quality-gates), **spec** — a PRD scaffold from course lessons 3–4 (problem statement template, solution-as-outcome, user stories, JTBD, constraints, verifiable acceptance criteria, out-of-scope, assumptions) scoped to product features since rooms have their own pipeline — plus review-setup and advise with a rule↔skill↔agent routing table. **Dry-run validated**: running the audit checklist immediately found a real defect (the forbidden phrase `это не просто` survives in 7 older theory files from pre-gate commits) and two false positives, which were fed back into the skill as a "distinguish history from current state" rule. (by Claude Code)
- [x] **Fork 3 resolved: theory headings are now gray, not emerald.** The owner's actual fix for the emerald-vs-aubergine clash. New `--color-heading` token (`#d4d4d4` dark / `#3f3f46` light, flipped in the SaaS block) applied via a `text-heading` utility: **250 replacements across 34 theory files**, regex-scoped to `h2`/`h3`/`h4` tags so emerald survives exactly where it means *interactive* — `<Term>` glossary links, buttons, progress, difficulty chips. Heading hierarchy now rests on size and weight. Recorded in DESIGN_FORKS(+ru) with the three alternatives kept live and one question still open (whether non-theory surfaces follow). Verified in browser: heading `rgb(212,212,212)` dark / `rgb(63,63,70)` light, terminal aubergine in both, glossary link still green. `check-all` green (1769). (by Claude Code)
- [x] **Fork 1 corrected: aubergine terminal restored; Fork 3 (heading color) opened.** The earlier "Tango on gray" move was a misread of the owner's intent — the owner wants the Ubuntu preset + Ubuntu Mono kept, and the emerald clash solved by re-coloring the **headings and similar accents** instead. Reverted the terminal container tokens to aubergine (`#300a24` family) in `globals.css`; DESIGN_FORKS(+ru) rewritten: aubergine back as current pick with the history note, Tango-on-gray demoted to a fully paste-ready alternative ("strongest fallback"), and a new **Fork 3 — theory heading color** recorded as OPEN with measured scope (`text-emerald-400`: 289 occurrences in theory, ~250 on h2/h3/h4; `<Term>` links excluded), an implementation plan (a `--color-heading` token + heading-scoped regex sweep) and three gray candidates. Picker: Ubuntu re-marked current and a **heading-color toggle** added (изумруд/светло-серый/цвет текста/приглушённый) so Fork 3 is judged by eye next to the aubergine terminal; prototype screenshot confirmed the clash disappears with light-gray headings. AGENTS.md element description restored. Verified in browser: termBg `rgb(48,10,36)` live. (by Claude Code)
- [x] **Fork 1 moved: terminal is now "Tango on gray"** (`#1e1e1e` graphite + Tango ANSI palette + Ubuntu Mono). The owner resolved the fork's open question — the aubergine background *does* clash with the site's emerald blocks. To judge replacements by eye, the picker gained the exact context that caused the clash (an emerald chapter heading + a "Куда дальше" callout inside the preview stage) and three gray candidates; side-by-side screenshots showed near-black `#0d0d0d` melting into the dark page background while `#1e1e1e` reads as a distinct surface — the owner picked gray. Swapped only the container tokens in `globals.css` (`bg/head/line/dim`); Tango accents, Ubuntu Mono, and the dark-in-both-themes invariant unchanged. Fork move recorded per the DESIGN_FORKS rules: new current pick with rationale, aubergine demoted to Alternative A with full paste-ready tokens and why it lost, Tango-on-black recorded as tried-and-rejected; AGENTS.md element description updated; picker notes/default now mark gray as current. Also: `docs` static-server entry added to `.claude/launch.json` (the picker needs http, not file://). Verified in browser: computed `#1e1e1e` in both themes, Tango prompt, Ubuntu Mono, 5 terminals on AC-201. `check-all` green (1769). (by Claude Code)
- [x] **New room `context-engineering-101` (AC-204, Module B, agent-coding path)** — closes a real curriculum gap: no room mentioned context engineering, CLAUDE.md-style constitution files, memory indexes, or skills. Sourced from the owner's course notes (Генаров course, lesson 2) but **authored original** — course slide listings were not copied; the concepts taught (layered context, PARA/Zettelkasten/progressive summarization, context rot, compaction) are public methodologies. 5 bilingual chapters: (1) the discipline + stable→volatile layer table, (2) window economics — minimal-maximally-useful principle, ROI test, context rot, delete>archive, with tool-specific numbers (60/200 lines, ~8k tokens) confined to a dated "Tool practice, 2026" callout per the fact-decay rule, (3) knowledge artifacts with the explicit rule/fact/procedure/role → constitution/index/skill/agent mapping + telegraph-style example, (4) skills and agents — description=when-to-invoke, when NOT to create a skill, minimal tools + explicit write zones, (5) hygiene cycle (audit/update/restructure/cleanup) with a `hygiene · zsh` Terminal session + compaction cross-linked to claude-code-agentic-loop; "Where to Go Next" CTA at the END per the CTA-placement gate. 12 tasks across 7 types (6 MCQ, sorting, categorize, multiple-select, mentor, scenario, input) — every answer stated verbatim in theory in both locales; categorize keys are English-exact. 2 new glossary terms (`context-engineering`, `context-rot`). Docs synced: CURRICULUM (+ru) AC-204 row, ROOM_DEVELOPMENT (+ru) shipped log, PROGRESS (+ru) milestone + counter, CLAUDE.md/AGENTS.md/README counters 39→40. Gate green: 1769 tests (room auto-added 55 checks). SSR verified in both locales; live DOM: 12 task cards, terminal renders, room in catalog. (by Claude Code)

## Completed (2026-07-20 session — by Claude Code)
- [x] **Glossary: 5 reliability-engineering terms + `<Term>` wiring in `agent-coding-foundations`.** Added bilingual entries to `src/data/glossary.ts` (simple-prose style, term after idea): `transient-failure`, `circuit-breaker`, `graceful-degradation`, `fault-tolerance`, `human-in-the-loop`. Wrapped first mentions in Chapter 6: «транзиентных ошибках»/"transient errors" in the recovery-order paragraph, Circuit Breaker in the fallback card, Human-in-the-Loop in the escalate card, Graceful Degradation + Fault Tolerance in the named-concepts paragraph (both locales; cards converted from string ternaries to JSX fragments where needed). Verified in DOM on `/ru` + `/en`: all five render as glossary spans (`cursor-help` dotted underline — the found-entry path in `Term.tsx`; a missing id would render a red "[Term not found]" marker, none present). `check-all` green (1714). (by Claude Code)
- [x] **`agent-coding-foundations`: recovery-stage cards rewritten in plain prose (creator's style guidance).** The four retry/fallback/rollback/escalate cards in Chapter 6's "Link to Classical Engineering" subsection rewritten per Sergei's instruction «Пиши проще» with his retry text as the model: unfold the everyday logic first (try again → why it helps → why not forever), introduce the terms after the idea, one thought per sentence. Both locales rewritten in the same register; no structural or factual changes; sources card and surrounding paragraphs untouched. Verified in browser on `/ru` + `/en` (new prose renders, old jargon-first phrasing gone). `check-all` green (1714). (by Claude Code)
- [x] **`agent-coding-foundations`: Chapter 6 "Errors and Recovery" extended with classical-engineering roots.** Existing structure, YAML schema, and the retry → fallback → rollback → escalate chain preserved untouched. Added a bilingual subsection "The Link to Classical Engineering / Связь с классической инженерией": (1) framing — the chain is distilled SE/Distributed Systems/DevOps/SRE practice, an agent is just another distributed system; (2) four stacked cards mapping each stage to its origin — retry → transient-fault handling with exponential backoff + hard attempt caps (retry storms), fallback → backup paths/degraded mode/Circuit Breaker, rollback → DB transactions, git reset/revert, blue-green/canary deploy rollback, escalate → Human-in-the-Loop as heir of SRE incident response/on-call with full-context handoff; (3) why this exact order — a recovery-cost ladder (cheap-first minimizes expected cost; per-rung limits prevent cascading failures); (4) named concepts paragraph — Graceful Degradation, Fault Tolerance, Resilience Engineering, Self-Healing Systems; (5) takeaway — agents reuse microservice resilience principles, agent = autonomous component with a service's contract (detect → contain → recover → escalate last with full context); (6) Sources card with 6 links: Azure Architecture Center (Retry, Circuit Breaker, Transient fault handling), Google SRE Book, Fowler (Circuit Breaker), AWS Well-Architected Reliability Pillar. Practical tone kept, no new `<Term>` wraps pending creator's pick. Verified in browser on `/ru` + `/en` (subsection renders, YAML intact, all 6 links present). `check-all` green (1714). (by Claude Code)

## Completed (2026-07-19 session — by Claude Code)
- [x] Roadmap M1-6 — GROK task pack (Month 1 now fully closed). Appended one Grok task to each of five rooms, each answerable from that room's existing theory with Grok as the concrete example (no fragile numbers; stable facts only — xAI, realtime X integration, Grok Vision, default persona): `llm-landscape#16` (MC, xAI tool-priced agentic-API strategy), `deep-search-agents#7` (multiple-select, realtime X-feed grounding — fresh but not authoritative, verify + cite), `native-multimodality#7` (MC with a new bar-chart SVG under `public/images/tasks/native-multimodality/` via the M1-4 image infra — native direct reading vs the old captioner chain), `prompting-101#11` (MC, override Grok's playful persona with a system prompt; tone ≠ temperature), `frontier-evals-logic#13` (MC, interpret a "Grok is #1" vendor benchmark claim critically). check-all green (1736); all five verified rendering in the browser, vision task screenshot-checked. (by Claude Code)

## Completed (2026-07-18 session — by Claude Code)
- [x] **Product screenshots documented as a core design element.** New mandatory section in `docs/AGENTS.md` (right after the Terminal rule) — the GUI counterpart of `<Terminal>`, modeled on TryHackMe's task pages (annotated VirusTotal screenshot framed between explanation paragraphs). Rules: real captures only (no mocked/AI-generated UI, no doctored numbers, crop + scrub personal data); mandatory "sandwich" structure (intro text above, interpretive text below — a bare screenshot is decoration); accent-border exhibit framing (`rounded-xl border-2 border-<accent>/60 overflow-hidden`, via `next/image`); dark-theme captures preferred (same logic as Terminal staying dark on light UI); assets under `public/images/rooms/<room-id>/` with descriptive names (tasks keep `public/images/tasks/<room-id>/`); bilingual `alt`/caption; solvability guard (screenshots illustrate, never spoil answers). Docs-only change — no runtime surface. (by Claude Code)
- [x] **`ai-history`: Dartmouth card historiographic revision (Kline 2011) + "birthplace myth" subblock + task 12.** Chapter 1, per archival literature: (1) Dartmouth funding concretized — ~$13,500 requested, Rockefeller officer Robert Morison approved $7,500 finding the plan vague/over-ambitious, framed as a documented vote of no confidence (inline DOI citation to Kline, "Cybernetics, Automata Studies, and the Dartmouth Conference on AI," IEEE Annals 33(4), 2011); (2) fixed the coining-of-"AI" motive — replaced the apocryphal "had to call it something" with McCarthy's "nail a flag to the mast" motive (distancing from cybernetics/Wiener, per Kline); (3) Logic Theorist card now notes it was built at RAND before and independently of Dartmouth, under the name "complex information processing"; (4) new bilingual subblock '"The birthplace of AI": a myth assembled after the fact' (AI@50, textbooks, McCorduck's *Machines Who Think* as myth-participating source). Added task 12 (multiple-choice) testing the myth subblock — solvable from theory. Verified in browser on `/ru` + `/en` (all new strings render, Kline link → DOI). `check-all` green (1714). (by Claude Code)
- [x] **`ai-history`: Halpern 2022 citation + new task.** Added a paginated scholarly citation to Chapter 3 of `AiHistoryTheory.tsx` — one bilingual paragraph placing the GOFAI-vs-connectionism dispute in intellectual history (learning-over-rules vs top-down calculation/planning), linking Orit Halpern, "The Future Will Not Be Calculated" (Critical Inquiry 48(2), 2022) via DOI with a page pinpoint (`Халперн 2022: 6` / `Halpern 2022: 6`; page number from the Russian edition's pagination). Reused the established inline-citation pattern (emerald underline `<a>`, `target="_blank"`). Added task 11 (multiple-choice, bilingual) to `tasks/ai-history.ts` testing exactly that paragraph — solvability gate satisfied. Verified in browser on `/ru` + `/en`: paragraph renders, link → DOI. `check-all` green (1710). (by Claude Code)

## Completed (2026-07-17 session — by Claude Code)
- [x] **Terminal design picker — `docs/terminal-picker.html`.** Makes Fork 1 explorable by eye instead of by hex, following the `green-accent-picker.html` precedent (self-contained HTML, RU UI). Switches 7 presets (Ubuntu/Tango current, neutral black-gray, Tango-on-black, Solarized Dark, Dracula, Nord, One Dark), 6 terminal fonts (Ubuntu Mono, IBM Plex Mono, JetBrains Mono, Fira Code, Source Code Pro, system) and 3 sizes — the size control exists because size is coupled to the face. Crucially it also toggles the **page background between the site's dark and light themes**, so the "terminal stays dark in both themes" invariant is verifiable visually. Renders the real AC-201 session (Cyrillic included — the thing to watch when judging a font) plus a palette terminal exercising `dir`/`link`/`warn`, and emits a paste-ready `@theme` token block with a copy button. Cross-linked from both `DESIGN_FORKS` files. Verified in browser: all 7 presets drive button state, computed terminal background and CSS output consistently. (by Claude Code)
- [x] **Committed orphaned docs that tracked files already referenced.** `docs/green-accent-picker.html`/`.md`, `docs/ROOM_DEVELOPMENT.md`/`.ru.md`, `docs/VERCEL.md` and `docs/references/` had been authored in earlier sessions but never committed — and `DESIGN_FORKS.md` (committed) links to the accent picker, so the link would have broken the moment the non-durable working tree re-synced. This is exactly the failure mode the Commit-hygiene gate exists to prevent. (by Claude Code)
- [x] **`agentic-cli-tools` (AC-201): introduced `rg` properly — closes a solvability-gate violation.** The owner flagged that `rg` appears out of nowhere in the Chapter 1 terminal with no explanation of what it is or how to get it. Investigation showed a worse problem underneath: **task #2 asks the learner to type "the command recommended in this project for fast code search instead of `grep`" (answer `rg`), but the theory never said any of that** — not that `rg` is ripgrep, not that it is recommended over `grep`. The task was only answerable by guessing from a terminal line. Fixes: new `ripgrep` glossary entry (bilingual, incl. install commands and the no-install fallback); a short first-contact note right after the Chapter 1 terminal wrapping `rg` in `<Term>` and pointing to Chapter 2; in Chapter 2 a full unpack of the four discovery commands — `rg` as ripgrep and explicitly *the one recommended instead of `grep`* (faster on large repos, respects `.gitignore`, skips binaries), `tree -L 2` (depth-capped directory tree), and `cat`/`git grep` noted as system-provided; a paragraph stating neither ripgrep nor `tree` ships with macOS/most Linux distros, with `grep -r` + `git grep` as the always-available fallback for locked-down environments; and a new `setup · zsh` terminal with the actual install commands (Homebrew / apt) plus a verify step. `check-all` green (1714); verified in browser — 5 terminals on the page, glossary tooltip resolves, `<code>` renders via `prose-invert`. (by Claude Code)
- [x] **Design decisions recorded as explicit open forks** — `docs/DESIGN_FORKS.md` (+ `.ru.md`). The owner flagged that the project is *still searching* for its optimal design, so terminal styling and typeface choices must not be recorded as settled. The file states up front that a "current pick" means *what is wired up today*, and carries **paste-ready complete token sets** for every option: Fork 1 (terminal design) — current Ubuntu/Tango, previous neutral black-gray, never-built Tango-on-black and Ubuntu+Ambiance-chrome, plus pre-converted Solarized Dark and Dracula presets; Fork 2 (typefaces) — site face (IBM Plex Sans vs Inter/Ubuntu Sans/split UI-reading) and terminal face (Ubuntu Mono vs IBM Plex Mono/JetBrains Mono/system), including the open tension that site and terminal currently use unrelated families, and the gotcha that terminal font size is coupled to the face (13→14px for Ubuntu Mono). Mandatory agent rules added: never treat a pick as final, never delete a losing option, record any fork move in the same commit, keep every preset complete (a partial preset silently breaks `text-term-*` utilities). Cross-linked from `AGENTS.md` (new "Design forks — do not silently collapse them" gate + a note on the Terminal design element) and the `CLAUDE.md` docs index. `check-all` green (1714). (by Claude Code)
- [x] **Terminal restyled to the Ubuntu GNOME Terminal look** (owner supplied a reference screenshot). Identified the reference as GNOME Terminal on Ubuntu (Unity-era Ambiance chrome) and adopted it wholesale: aubergine background `#300A24` (Ubuntu's signature terminal color), the **Tango** ANSI palette (`#EEEEEC` text, `#8AE234` prompt, `#EF2929` red, plus new `--color-term-blue/-cyan/-yellow` tokens matching Ubuntu's `LS_COLORS`, so `ls`-style output can be rendered faithfully), and **Ubuntu Mono** loaded via `next/font/google` in `layout.tsx` (latin+cyrillic — RU comments live in terminals) exposed as the `--font-term` token → `font-term` utility. `Terminal.tsx` gained the extra tones (`dir`/`link`/`warn`) and bumped to 14px for optical parity with the narrower Ubuntu Mono glyphs. All `--color-term-*` remain un-overridden in the SaaS block. Verified in browser: computed bg `rgb(48,10,36)`, text `rgb(238,238,236)`, prompt `rgb(138,226,52)`, font resolves to real "Ubuntu Mono", Cyrillic renders, aubergine holds in the light theme, no console errors. `check-all` green (1714). (by Claude Code)
- [x] **Per-room "Reset progress" button.** Added `resetProgress` to `useProgress` (clears local state + `localStorage.progress:<room>`, and `DELETE /api/progress/<room>` when authed — the backend endpoint already existed). Wired into the room-page progress card: a subtle `RotateCcw` "Сбросить прогресс / Reset progress" button that appears only when the room has progress, with an inline confirm step (Да·Отмена / Yes·Cancel). Because task cards keep their solved/answer state in internal `useState` (via `initialCompleted`), reset bumps a `resetNonce` folded into each task's React `key` so the cards remount to a fresh idle state; also resets the completion-modal guard. Verified in browser: seeded progress → 14%, button shown → confirm → "Yes" → 0%, `localStorage` cleared, button hidden. `check-all` green (1706). (by Claude Code)
- [x] **Terminal promoted to a core design element + rolled out to 5 more rooms.** Documented `<Terminal>` as a first-class design-system component in `docs/AGENTS.md` (right after Design tokens): API, when to use (command/agent/REPL/test sessions), when NOT (static JSON/YAML/schema/math stay plain code blocks), and a solvability guard (a terminal must not reveal a task's answer). Added tailored session terminals to `agent-coding-foundations` (agent executing the YAML contract → tool calls → acceptance check), `agentic-coding-tools` (read_file→edit_file→run_tests), `mcp-tool-ecosystems` (`claude mcp add` → tool call through the server), `fine-tuning-101` (LoRA training run: pip install → train → loss/epochs → saved adapter), `embeddings-101` (Python REPL: encode → cosine similarities). Terminal is now present in **11 rooms**. `check-all` green (1706); SSR confirms 1 terminal in each new room (ru). (by Claude Code)
- [x] **Reusable `<Terminal>` component + rollout to 5 more rooms.** Extracted the AC-201 terminal markup into `src/components/Terminal.tsx` (lang-agnostic; callers pass resolved bilingual strings; line kinds `{ cmd, comment?, prompt? }` / `{ out, tone? }` where tone `dim`|`ok`|`bad`). Added a `--color-term-red` token (`#f87171`, also not overridden in the SaaS block) for red→green output. Refactored AC-201 to use the component and upgraded its three plain `<pre>` "CLI Commands" blocks (Discovery/Change/Verify) into real terminals. Placed a tailored terminal in each of: `llama-3-1-8b` (Ollama install→pull→run, converted the existing plain block), `local-models-101` (Ollama first-run install→pull→run→verify in Chapter 4), `claude-code-agentic-loop` (Gather→Act→Verify one-loop pass, matching the chapter's own phase names, both lang branches), `claude-code-pro-workflow` (Claude Code `/plan`→execute→test session, both lang branches), `agentic-testing-loop` (red→green: failing `npm test` → minimal fix → green, Chapter 2). Also fixed a pre-existing forbidden-phrase violation (`это не просто`) in `ClaudeCodeProWorkflowTheory.tsx` found while editing. Verified: `check-all` green (1706); SSR HTML shows correct terminal counts across all 6 rooms (AC-201=4, others=1) in ru+en; live DOM confirmed termBg `#0d0d0d`, red `#f87171`, prompt `#34d399`; terminals stay dark in the SaaS light theme (no `--color-term-*` override there). (by Claude Code)
- [x] `agentic-cli-tools` (AC-201): added a styled **black-gray terminal window** to Chapter 1 (traffic-dot header, `$` prompt, monospace) showing one concrete discover→change→verify→commit loop pass, bilingual comments. Added dedicated `--color-term-*` tokens to `globals.css` `@theme` that are **not** overridden in the `[data-theme="saas"]` block, so the terminal stays dark on both themes (a terminal should look like a terminal on light UI too). Verified in browser: bg `#0d0d0d`, emerald prompt, gray dots — dark in both Terminal and SaaS themes. `check-all` green (1706). (by Claude Code)
- [x] `ai-history` room: added the missing **Expert Systems (1980s)** piece to Chapter 2 (a bilingual sub-block: first commercial AI success — XCON at DEC, LISP machines, expert-system shells — and how its brittleness + costly upkeep + cheap general workstations collapsed the market ~1990, causing the second AI Winter). This also closes a solvability gap: the room's `categorize` task already mapped "Expert Systems → Symbolic AI (GOFAI)" and the scenario mission referenced "expert-system algorithms" and the "second AI Winter", but the theory never introduced them. `check-all` green (1706); rendered/verified in browser; no forbidden phrases. (by Claude Code)

## Completed (2026-07-16 session — by Claude Code)
- [x] Roadmap M1-5 (pilot, awaiting creator approval): shipped the first task illustrations on the M1-4 image infrastructure. Three design-token SVG concept diagrams under `public/images/tasks/<room-id>/`: CLI discover→change→verify loop (`agentic-cli-tools#1`), 78/22 context auto-compact buffer bar (`claude-code-pro-workflow#5`), and the Gather→Act→Verify agentic loop (`claude-code-agentic-loop#8`). Attached to CONTEXT tasks, not to `agentic-loop#1` (name the phases, MC) or `agentic-cli-tools#4` (order the loop, sorting) where a diagram would reveal the answer — solvability gate respected. Bilingual alt/caption on each; rendered and screenshot-verified in context; `check-all` green (1706). Real terminal screenshots deferred (cannot be authentically captured here). Note: a pre-existing `TaskTimeline` SSR hydration warning surfaced during verification (reproduces in untouched rooms too) — logged as a follow-up, not caused by this change. (by Claude Code)
- [x] Roadmap N-2: merged PR #1 into `main` (merge commit `04da99b`) — root `CLAUDE.md`, `local-models-101` security chapter, responsive mobile shell, `taxonomy-matching` + `prompt-evals` rooms, 47 data-defect fixes, armed test gate. Working branch fast-forwarded onto `main` (no history rewrite). (by Claude Code)
- [x] Roadmap M1-4: task-image infrastructure. Optional `image?: { src, alt: LocalizedString, caption?: LocalizedString }` on `LocalizedTask` (+ resolved form on `Task`); locale resolution in the room page; shared `TaskIllustration` component (exported from `TaskWrapper.tsx`) rendered above the question in all 8 task types (`TaskWrapper` path + own-markup `TaskMentor`/`TaskScenario`); asset convention `public/images/tasks/<room-id>/` (+ `.gitkeep`); new Vitest block in `data-integrity.test.ts` (src must exist under `public/`, alt/caption bilingual); rule documented in `AGENTS.md` gate. Acceptance verified both ways: temp image rendered with localized alt/caption at `/en` and `/ru`, and a deliberately broken path failed `npm run test` with a clear message. Gate green: 1701/1701. (by Claude Code)

## Completed (2026-07-15 session — by Claude Code)
- [x] Roadmap M1-1: cleared all 47 tracked data defects. 22 `categorize` tasks — `correctMapping` normalized to canonical form (keys = `items[].en`, values = `buckets[].en`; RU-keyed and duplicated bilingual entries removed — the renderer silently dropped them, making items unsolvable). 6 `scenario` tasks (`prompt-contracts#5`, `multi-agent-collaboration#5`, `agentic-testing-loop#4`, `agentic-ui-delivery#5`, `agentic-swarm-management#4`, `frontier-evals-logic#8`) — scores rescaled from a 0–10 scale to the canonical 0–100 (×10, negatives clamped to 0, `passingScore` 8→80 / 9→90). 19 tasks — missing bilingual `explanation` texts authored (takeaway summaries for the success box). The "Engineering follow-ups uncovered by tests" list below is now fully resolved. (by Claude Code)
- [x] Roadmap M1-2: restored the `prompt-evals` room — created `src/data/rooms/tasks/prompt-evals.ts` with 9 tasks across 7 types (multiple-choice ×3, input, multiple-select, categorize, sorting, scenario, mentor), all answerable from `PromptEvalsTheory.tsx` (vibe check trap, three eval types, Release Gate, growing eval set), registered in `tasks/index.ts`. Room inventory now 39/39 fully wired. Verified rendering in both locales. (by Claude Code)
- [x] Roadmap M1-3: armed the test gate — `npm run test` added to `check-all` (`lint` + `tsc --noEmit` + Vitest). Full pipeline green: 1700/1700 tests. (by Claude Code)
- [x] New room `taxonomy-matching` (Intermediate, "Practice" category) — "Matching Jobs to a Skill Taxonomy". Teaches entity resolution / semantic matching as an applied ML task, framed on a real labor-market case: matching ~27k messy EN job titles to a curated 154-profession RU taxonomy. 5 bilingual theory chapters (messy-text-vs-taxonomy, normalization, lexical matching, semantic retrieval with multilingual embeddings, cascade + LLM adjudication + precision/recall eval) and 10 tasks across 7 types (multiple-choice, multiple-select, sorting, categorize ×2, input, scenario, mentor). Wired into metadata + `ROOM_TASKS` + `THEORY_COMPONENTS`. Room count 38→39 (38 wired). All 42 room tests pass in both locales; `check-all` clean; verified rendering and a task solved on a touch viewport. (by Claude Code)
- [x] Hardened `TaskCategorize` tap-to-place for multi-item buckets. After the N-1 fix, tapping a bucket that already contained a chip could land on the chip's remove handler (chips render inside the bucket) instead of adding the selected item — exposed by the new room's 3-items-per-bucket task. Now a tap on a placed chip while an item is selected places the selection into that chip's bucket; removal still works when nothing is selected. Verified: 3-per-bucket and 1-per-bucket categorize tasks both solve on a touch viewport. (by Claude Code)
- [x] Roadmap N-1: fixed touch-drag on `TaskCategorize`. It used native HTML5 drag (`draggable`/`onDragStart`/`onDrop`), which does not fire on touchscreens — categorize tasks were unsolvable on mobile. Added a tap-to-place interaction (tap an item to select, tap a category to place; tap a placed item to remove) alongside the existing native drag, with `aria-pressed`/`role="button"`/keyboard (Enter/Space) support. Subtitle updated in both locales ("Tap an item, then a category (or drag)"). Verified end-to-end on a 375px `hasTouch` viewport (Chromium): full local-models-101 family→company task solved via taps → "Correct!"; `check-all` clean. (by Claude Code)

## Completed (2026-07-12 session — by Claude Code)
- [x] Made the app shell responsive for mobile. The layout was a fixed CSS grid with an always-visible 208px sidebar and no `@media` queries, so on a phone the sidebar squeezed content into ~167px. Now, below `md` (768px): the sidebar renders as an off-canvas drawer (`fixed`, slide-in) toggled by a new hamburger button in `Navbar`, with a tap-to-dismiss backdrop and auto-close on link/logout; the shell grid collapses to a single full-width column. Desktop (`md+`) is byte-for-byte unchanged (sticky 208/64px sidebar, no hamburger). Touched `AppShell.tsx`, `Sidebar.tsx`, `Navbar.tsx`; `check-all` clean; verified with headless Chromium at 375px (drawer off-screen, full-width content, open/close/navigate) and 1280px (desktop intact). (by Claude Code)
- [x] Added a root `CLAUDE.md` (the file Claude Code auto-loads) as the authoritative AI-assistant entry point: architecture, dev commands, project structure, key patterns, task types, testing, and the mandatory authoring gates distilled from `AGENTS.md`. Corrected drift (compete/leaderboard are now "coming soon" stubs; labs backend router; 38 rooms / 37 wired). (by Claude Code)
- [x] Extended the `local-models-101` room with a security-research use-case chapter (Chapter 6, EN/RU): a privacy/offline/cost/reproducibility/customization/transparency motivations table and a curated defensive-security reading list (Hackphyr, ForensicLLM, Red Teaming LMs, and more), plus a new comprehension task (`#11`, room now 11 tasks) that reinforces the honest trade-off — local models lead on control/privacy/reproducibility, not raw capability, and authorization is always required. All `local-models-101` Vitest checks pass in both locales; `check-all` clean; room verified rendering at `/en` and `/ru`. (by Claude Code)

## Completed (2026-05-16 session — by Claude Code)
- [x] Full project review (docs + codebase) and a 3-month agent roadmap: `ROADMAP_3M.md` + `ROADMAP_3M.ru.md`. Month 1 — fix 47 data defects, restore `prompt-evals`, arm the test gate, task-image infrastructure, pilot screenshots, GROK task pack; Month 2 — complete Agent Coding path (AC-301…AC-402), screenshot rollout, backend pytest smoke tests; Month 3 — leaderboard, spaced repetition, analytics, stretch items. (by Claude Code)
- [x] Shipped `local-models-101` room — Beginner entry point of the «Открытые модели / Open Models» category (5 theory chapters with `<Term>` tooltips, 10 tasks across 8 task types, all passing the Vitest gate in both locales; 3 new glossary terms: `open-weights`, `quantization`, `vram`). `llama-3-1-8b` stays the model-specific deep dive; docs synced (README, PROGRESS, CURRICULUM, AGENTS, CLAUDE). (by Claude Code)

## Completed (2026-05-11 session — by Claude Code)
- [x] Stood up the first test layer: Vitest + `vite-tsconfig-paths`, `vitest.config.ts`, npm scripts (`test`, `test:watch`, `test:coverage`), and two suites under `src/data/rooms/__tests__/` (data integrity + per-task-type validation, 1527 assertions total) codifying the rules from `AGENTS.md` → "Task data validation gate" and "Task ID sequencing". (by Claude Code)
- [x] Added `TESTING.md` capturing the layered strategy, what is and isn't covered, and a triage list of 47 real data defects the initial run surfaced (22 broken `categorize` mappings, 6 unsolvable `scenario` tasks, 17 empty `explanation` fields, 1 unregistered room — see Engineering follow-ups below). (by Claude Code)

## Engineering follow-ups uncovered by tests (2026-05-11)

> **Resolved 2026-07-15 (by Claude Code):** every defect in this section was fixed as roadmap M1-1/M1-2, and `npm run test` is now part of `check-all`. The list below is kept for history.

Captured here as a self-contained punch list. Re-run `npm run test` after each fix to confirm the item drops off. When the list is empty, re-add `npm run test` to `check-all` in `package.json`.

### 🔴 Critical — `prompt-evals` room is broken in production (1)
Present in `ROOMS_METADATA` but absent from `ROOM_TASKS` and from `src/data/rooms/tasks/` — clicking the room lands on an empty/broken page. `PROGRESS.md` 2026-02-17 milestone says the room once had 6 tasks; either restore the task file or remove the metadata entry.
- [ ] `prompt-evals` — restore `src/data/rooms/tasks/prompt-evals.ts` and add the import to `tasks/index.ts`, **or** remove from `ROOMS_METADATA` and from any path that references it.

### 🔴 Critical — `categorize` tasks unsolvable due to non-English `correctMapping` keys (22)
The room renderer at `src/app/[lang]/rooms/[id]/page.tsx:138-144` matches `correctMapping` keys/values against `items[i].en` and `buckets[j].en`. Non-English or mismatched keys produce an empty resolved mapping, so **no drop ever registers as correct** for either locale. Fix: rewrite each `correctMapping` so every key exactly equals some `items[i].en` and every value exactly equals some `buckets[j].en`.
- [ ] `agent-coding-foundations#6`
- [ ] `agent-coding-foundations#7`
- [ ] `agentic-swarm-management#6`
- [ ] `agentic-testing-loop#3`
- [ ] `agentic-ui-delivery#3`
- [ ] `ai-agents#4`
- [ ] `ai-history#8`
- [ ] `ai-image-creation#5`
- [ ] `ai-rag#3`
- [ ] `ai-regulation-eu#2`
- [ ] `ai-regulation-ru#3`
- [ ] `ai-security#4`
- [ ] `chatgpt-moment#8`
- [ ] `claude-code-agentic-loop#2`
- [ ] `frontier-evals-logic#6`
- [ ] `llm-guardrails#2`
- [ ] `llm-mechanics#11`
- [ ] `multi-agent-collaboration#4`
- [ ] `native-multimodality#5`
- [ ] `prompt-contracts#4`
- [ ] `prompting-101#9`
- [ ] `scaling-hypothesis#6`

### 🔴 Critical — `scenario` tasks unsolvable (6)
Either no choice meets `passingScore` (default `60`), or a choice has a score outside `[0, 100]`. The user cannot complete the scenario. Fix: review the scoring rubric, ensure at least one choice meets `passingScore`, and clamp every `score` to `[0, 100]`.
- [ ] `prompt-contracts#5` — no choice meets `passingScore=60`
- [ ] `multi-agent-collaboration#5` — no choice meets `passingScore=60`
- [ ] `agentic-testing-loop#4` — no choice meets `passingScore=60`
- [ ] `agentic-ui-delivery#5` — no choice meets `passingScore=60`
- [ ] `agentic-swarm-management#4` — negative score (`-5`)
- [ ] `frontier-evals-logic#8` — negative score (`-5`)

### 🟡 Low severity — Empty `explanation` fields (18)
`explanation` is contract-required `LocalizedString`, but several tasks ship `{ en: '', ru: '' }`. UI renders an empty feedback block — the task still completes, but post-task feedback is missing. Fix: write a 1–2 sentence explanation per task, in both locales.
- [ ] `agentic-swarm-management#9`
- [ ] `ai-alignment#6`
- [ ] `ai-history#6`
- [ ] `ai-regulation-eu#5`
- [ ] `ai-regulation-ru#5`
- [ ] `ai-research#6`
- [ ] `ai-singularity#6`
- [ ] `chatgpt-moment#5`
- [ ] `claude-code-agentic-loop#9`
- [ ] `deep-search-agents#6`
- [ ] `fine-tuning-101#8`
- [ ] `llm-guardrails#5`
- [ ] `llm-landscape#11`
- [ ] `llm-mechanics#8`
- [ ] `post-chatgpt-history#6`
- [ ] `prompting-101#6`
- [ ] `research-grounding#6`
- [ ] `scaling-hypothesis#5`
- [ ] `llama-3-1-8b#7` — found 2026-05-16 after the initial triage (room shipped after `TESTING.md`); test suite now reports 48 failures total, all tracked here

### 🟢 Gate — Fold tests into `check-all`
- [ ] Re-add `npm run test` to `check-all` in `package.json` once all items above are green. Until then, `check-all` stays at `lint && tsc --noEmit` so unrelated work isn't blocked by pre-existing red.

## Completed (2026-05-07 session — by Claude Code)
- [x] Shipped production deploy `1b6588e` to Vercel (`ai-learning-platform-murex.vercel.app`): 151 files, +12331/-481, including all backlog items below. (by Claude Code)
- [x] Added `/${lang}/compete` and `/${lang}/leaderboard` pages to back the existing sidebar routes. (by Claude Code)
- [x] Added `/${lang}/faq` page covering platform basics, auth, progress/streak, troubleshooting, and privacy. (by Claude Code)
- [x] Added 5 new Agent Coding rooms with theory components + task files: `agentic-swarm-management`, `claude-code-agentic-loop`, `claude-code-pro-workflow`, `mcp-tool-ecosystems`, `frontier-evals-logic`. (by Claude Code)
- [x] Added `PromptPlayground` and `AIProfessionCard` components plus `src/data/rooms/playgroundConfigs.ts` data. (by Claude Code)
- [x] Repo hygiene: normalized `.gitignore` (`.next/`, `__pycache__/`, `*.pyc`), untracked previously-tracked Python cache files, removed stray `backend/=1.40.0` pip artifact. (by Claude Code)

## Completed
- [x] Moved `/${lang}/rooms` difficulty/focus/status filters into a dedicated desktop sidebar rail with sticky placement and a compact result summary. (by Codex)
- [x] Increased `/${lang}/rooms` density on large screens with a wider container, more columns, and tighter card spacing. (by Codex)
- [x] Reduced the `/${lang}/rooms` cover area: smaller room-symbol blocks, shorter headers, and tighter top-card composition. (by Codex)
- [x] Reworked `/${lang}/rooms` cards to use professions-style illustrated covers with room icons and real progress badges. (by Codex)
- [x] Added the `/${lang}/professions` page with search, filters, profession cards, and sidebar navigation for AI role discovery. (by Codex)
- [x] Added `ROADMAP_VIEW_MODE.md` and `ROADMAP_VIEW_MODE.ru.md` to capture the roadmap-style viewing mode for future AI Learn trajectory and progression screens. (by Codex)
- [x] Added the `ai-career-trajectories` room with a custom roadmap-style theory layout, bilingual IC/research/management track content, and 6 validated tasks. (by Codex)
- [x] Enriched theory content (1000+ words each) for rooms: ai-history, research-grounding, ai-research, prompting-101, ai-image-creation.
- [x] Create "Post-ChatGPT Era" room page and tasks (6 tasks).
- [x] Scaffold Next.js 16 project with TypeScript/Tailwind.
- [x] Implement TryHackMe-style Dashboard and Sidebar.
- [x] Create Room Layout (Content + Task Sidebar).
- [x] Build interactive `TaskQuestion` component (Input, MC, Multi-select).
- [x] Build interactive `TaskSorting` component (drag-to-reorder).
- [x] Implement Multi-language support (RU/EN) with Middleware.
- [x] Create "Room 101: LLM Landscape" with Geopolitics block (26 tasks).
- [x] Implement Learning Path overview and detail pages.
- [x] Create "Room 102: How LLMs Think" with tokens, prediction, context, temperature (4 tasks).
- [x] Create Rooms index page (`/[lang]/rooms`) with all rooms listing.
- [x] UI redesign: Linear/Notion aesthetic — muted palette, no neon/glow/emoji, increased whitespace.
- [x] Dashboard rework: compact room rows, "Continue learning" primary CTA.
- [x] Create "Room 103: Prompting 101" page and tasks (6 tasks).
- [x] Create "Room 201: Native Multimodality" page and tasks (6 tasks).
- [x] Create "AI History" room page and tasks (6 tasks).
- [x] Create "Prompt Evaluation & Evals" room page and tasks (6 tasks).
- [x] Create "AI for Image Creation" room page and tasks (6 tasks).
- [x] Persist task progress in localStorage (`useProgress` hook).
- [x] Show real progress on rooms list page (Completed / In Progress / Not Started badges).
- [x] Show real progress on dashboard (completed room count + progress bar).
- [x] Fix TaskSorting `disabled` prop type error for Framer Motion compatibility.
- [x] Deploy to Vercel.
- [x] Apply consistent green accent styling across all primary flows (Dashboard, Paths, Rooms, active sidebar route states).
- [x] Create path "Идеи и споры об ИИ / Ideas and Debates in AI" (`/${lang}/paths/ideas-history`).
- [x] Create "ChatGPT moment" room page and tasks (6 tasks).
- [x] Create "Сингулярность в AI-дебатах / Singularity in AI Debates" room page and tasks (6 tasks).
- [x] Create "Room 202: Research & Grounding" page and tasks (6 tasks).
- [x] Add "Success Modal" upon room completion with confetti and stats.
- [x] Centralize room metadata in one source (`src/data/rooms/`) for title, difficulty, tasks, and duration.
- [x] Create "Alignment: AI Alignment & RLHF" room page and tasks (6 tasks).
- [x] Implement `/${lang}/settings` page with language preference + per-room progress reset.
- [x] Refresh "LLM Landscape" room question set to a concise structured quiz (10 tasks).

## Completed (2026-04-05 session — by Codex)
- [x] Implemented 4 new Agent Coding rooms: AC-103 (Prompt Contracts), AC-104 (Multi-Agent Collaboration), AC-202 (Agentic Testing Loop), and AC-203 (Agentic UI Delivery). Each room includes 5 theory chapters and 10 localized tasks. (by Codex)
- [x] Refined the AC-101 (`agent-coding-foundations`) Chapter 1...
- [x] Added `DEC-002-EXPLAINED.md` with a concise Russian walkthrough of the accepted DEC-002 autonomous pipeline: autonomy levels, safety model, rollout stages, review/UAT loops, and Digital Twin operating model. (by Codex)
- [x] Added a new in-progress backlog task to implement DEC-002 ideas in AI Learn Platform through an autonomous terminal-session pipeline track. (by Codex)
- [x] Fixed corrupted UTF-8 symbols in AC-101 theory text (`agent-coding-foundations`) so Russian words render correctly in the room UI. (by Codex)
- [x] Added a thematic focus filter on `/${lang}/rooms` with presets (`Agent Coding`, `AI Philosophy`) and dynamic options for all room categories; wired into room listing logic alongside difficulty/status filters. (by Codex)
- [x] Reworked AC-201 (`agentic-cli-tools`) theory into a more practical lesson format with explicit loop model, concrete CLI examples by phase, verify tooling matrix, role split, and final checklist/exercise in RU/EN. (by Codex)

## Completed (2026-04-04 session — by Codex)
- [x] Implemented Agent Ops MVP in Labs: added `/${lang}/labs/agent-ops` with bilingual UI for task queue, cycle trigger, runs feed, and knowledge feed. (by Codex)
- [x] Added backend Agent Ops API (`/agent/tasks`, `/agent/tasks/{task_id}/status`, `/agent/cycle/run`, `/agent/runs`, `/agent/knowledge`) with queue processing and dedupe-by-fingerprint knowledge storage. (by Codex)
- [x] Added database schema for Agent Ops (`agent_tasks`, `agent_runs`, `agent_knowledge_items`) with Alembic migration and synced docs in `README(.ru).md` and `PROGRESS(.ru).md`. (by Codex)

## Completed (2026-03-29 session — by Codex)
- [x] Implemented AC-102 room `agentic-coding-tools` (5 theory chapters + 10 localized tasks), including metadata/task wiring, dynamic theory mapping, and path inclusion under `agent-coding`. (by Codex)
- [x] Synced room inventory/status docs for the new room in `README(.ru).md`, `PROGRESS(.ru).md`, `CURRICULUM(.ru).md`, `BACKLOG(.ru).md`, and `AGENTS.md`. (by Codex)
- [x] Implemented AC-201 room `agentic-cli-tools` (5 theory chapters + 10 localized tasks) and wired it into the Agent Coding path with dynamic room routing. (by Codex)
- [x] Updated Agent Coding planning/docs for AC-201 in `README(.ru).md`, `PROGRESS(.ru).md`, `CURRICULUM(.ru).md`, `BACKLOG(.ru).md`, and `AGENTS.md`. (by Codex)

## Completed (2026-03-28 session — by Codex)
- [x] Implemented guest progress sync on signup in `useAuth`: reads valid `localStorage` `progress:*` entries and replays tasks to `/api/progress/{roomId}` after successful account creation. (by Codex)
- [x] Synced docs and status tracking for this behavior in `README(.ru).md`, `PROGRESS(.ru).md`, and `BACKLOG(.ru).md`; marked the engineering backlog item as done. (by Codex)
- [x] Added glossary tooltip support for the term "Code Red" / "Красный код" in Post-ChatGPT Era Chapter 1 and synced EN/RU docs. (by Codex)
- [x] Added a dedicated cover image for `post-chatgpt-history` (`/images/post-chatgpt-era.webp`) and updated EN/RU docs and work logs. (by Codex)
- [x] Added a dedicated cover image for `ai-singularity` (`/images/ai-singularity.avif`) and updated EN/RU docs and work logs. (by Codex)
- [x] Moved Skills Matrix to profile page `/${lang}/settings`, switched sidebar entry to “Profile”, and added redirect from `/${lang}/skills` to `/${lang}/settings#skills-matrix`. (by Codex)
- [x] Reworked `/${lang}/rooms` from stacked list to responsive tile grid (3/2/1 columns), added equal-height image-first cards, hover lift/accent border behavior, and filter controls for difficulty/progress status. (by Codex)
- [x] Refined room card metadata on `/${lang}/rooms`: moved difficulty/status from image overlays into a compact minimalist row above each room title. (by Codex)
- [x] Fixed hydration mismatch on `/${lang}/rooms` by making initial progress status SSR-safe and syncing localStorage progress only after client mount. (by Codex)
- [x] Created a new grand-task epic for a multi-room `agent-coding` learning path and synced planning docs (`BACKLOG(.ru).md`, `CURRICULUM(.ru).md`). (by Codex)
- [x] Implemented AC-101 room `agent-coding-foundations` (5 theory chapters, 10 localized tasks), wired new path route `/${lang}/paths/agent-coding`, and added glossary terms `guardrails` + `context-window`. (by Codex)

## Completed (2026-03-24 session — by Codex)
- [x] Migrated Next.js locale request handler from `src/middleware.ts` to `src/proxy.ts` (`export function proxy`) to match Next.js 16 file conventions and remove the deprecation warning. (by Codex)
- [x] Synced docs for this platform-level change in `README(.ru).md`, `PROGRESS(.ru).md`, `CURRICULUM(.ru).md`, and `BACKLOG(.ru).md`. (by Codex)
- [x] Shipped frontend style pass (Notion-like): refined typography stacks, added `reading-prose` for long theory text, and introduced `content-shell` spacing rhythm in the app shell. (by Codex)
- [x] Softened base visual system tokens (`base/card/input/border`) and tuned Navbar/Sidebar density for calmer reading-focused UI. (by Codex)

## Completed (2026-03-22 session — by Claude Code)
- [x] Split monolithic `src/data/rooms.ts` (5,143 lines) into `src/data/rooms/` directory: `types.ts`, `paths.ts`, `metadata.ts`, and 22 per-room task files under `tasks/`. Barrel re-export preserves all existing imports. (by Claude Code)
- [x] Extracted design tokens into Tailwind v4 `@theme` block in `globals.css`: 9 color tokens for surfaces and borders. Replaced ~680 hardcoded hex values (`border-[#262626]`, `bg-[#1a1a1a]`, `bg-[#141414]`, etc.) across 40+ component files with semantic classes (`border-border-card`, `bg-card`, `bg-card-dark`, etc.). (by Claude Code)
- [x] Extracted `TaskWrapper` component from 4 task components (TaskQuestion, TaskSorting, TaskCategorize, TaskTimeline). Shared container, icon header, success/error feedback now defined once. (by Claude Code)

## Completed (2026-03-19 session — by Codex)
- [x] Refined `post-chatgpt-history` Chapter 1 to a less stylized presentation: replaced desktop two-column card layout with a single-column flow, removed decorative glow emphasis, and removed italicized closing paragraph style in EN/RU. (by Codex)
- [x] Synced docs for this content/UI adjustment in `README(.ru).md`, `PROGRESS(.ru).md`, and `CURRICULUM(.ru).md`. (by Codex)
- [x] Added `Anti-Vibecode Frontend Gate (Mandatory)` to `AGENTS.md` with 10 pass/fail rules, a vibecode-marker removal list, and a deterministic pre-ship checklist. (by Codex)
- [x] Synced policy update logs in `README(.ru).md`, `PROGRESS(.ru).md`, and `BACKLOG(.ru).md`. (by Codex)

## Completed (2026-03-18 session — by Gemini)
- [x] Created room: Guardrails: Safeguarding AI (`llm-guardrails`) — 6 tasks incl. categorize, sorting, scenario, mentor (by Gemini).
- [x] Created room: AI Regulation in Russia 2026 (`ai-regulation-ru`) — 6 tasks incl. categorize, sorting, scenario, mentor (by Gemini).
- [x] Created room: EU AI Act: The Global Standard (`ai-regulation-eu`) — 6 tasks incl. categorize, sorting, scenario, mentor (by Gemini).

## Completed (2026-03-17 session — by Codex)
- [x] Updated AI History theory Chapter 1 event cards from desktop two-column layout to single-column flow per UI request. (by Codex)
- [x] Synced docs for this UI behavior update in `../README.md`, `../README.ru.md`, `PROGRESS.md`, and `PROGRESS.ru.md`. (by Codex)
- [x] Removed italic styling from the AI History Chapter 1 reflective paragraph per UI request and synced docs updates. (by Codex)
- [x] Enriched ChatGPT Moment theory Chapter 3 and Chapter 4 "Code Red" section in both locales, then synced `README`, `PROGRESS`, and `CURRICULUM` mirrors. (by Codex)
- [x] Updated Singularity in AI Debates Chapter 2 comparison cards from two columns to a single-column flow and synced EN/RU docs. (by Codex)
- [x] Added AI History glossary tooltips for `Eliezer Yudkowsky`, `Ray Kurzweil`, `John von Neumann`, `I.J. Good`, and `Nick Bostrom`, plus synced EN/RU docs. (by Codex)
- [x] Removed centered text alignment from the AI History final summary block and synced EN/RU docs. (by Codex)
- [x] Expanded the LLM Landscape pre-model chapter intro ("2026 Market Snapshot + Source Links") with practical vendor-evaluation context and synced EN/RU docs. (by Codex)
- [x] Added an `SDK` tooltip in LLM Landscape evaluation criteria and added AGENTS rule forbidding the word `вендор` with neutral alternatives. Synced EN/RU docs. (by Codex)

## Completed (2026-03-17 session — by Claude Code)
- [x] Enriched `scaling-hypothesis` room: rewrote theory to 5 full chapters (The Core Idea, Three Pillars, Emergent Abilities, Chinchilla Laws, Critics & New Frontiers), fixed broken task id (10→5), expanded from 5 to 10 tasks adding categorize, scenario, sorting, timeline, multiple-select task types. (by Claude Code)
- [x] Enriched `ai-singularity` room: expanded theory from 2 slim sections to 5 full chapters (Event Horizon, Two Camps, Physical Limits, Key Voices, Practical Agenda), expanded from 6 to 10 tasks adding timeline, scenario, multiple-choice, multiple-select task types. (by Claude Code)
- [x] Rewrote `ai-history` theory (AiHistoryTheory.tsx) incorporating 10 insights from Melanie Mitchell's 2019 book: Dartmouth reality, Minsky vs Rosenblatt story, Simon/Minsky wrong predictions, Deep Blue + McCarthy quote, Hinton 1990 job advice, LeCun ImageNet quote, AlphaGo kami no itte, NVIDIA stock, adversarial examples warning, Hofstadter Google/EMI story. (by Claude Code)

## Completed (2026-03-16 session — by Gemini)
- [x] Enriched LLM Mechanics room (to 14 tasks) and AI Image Creation room (to 7 tasks) with new interactive components and deeper technical theory. (by Gemini)
- [x] Expanded AI History room with detailed Lighthill Report and GPU/NVIDIA context. Integrated Geoffrey Hinton's legacy and safety concerns into History and Singularity rooms. (by Gemini)
- [x] Enriched Scaling Hypothesis room (to 9 tasks) and AI Singularity room (to 10 tasks) with new interactive components. (by Gemini)
- [x] Enriched ChatGPT Moment room (to 10 tasks) with new interactive components (Timeline, Categorize, Scenario, Sorting). (by Gemini)
- [x] Enriched Prompting 101 room (to 10 tasks) with new interactive components (Timeline, Categorize, Scenario, Sorting). (by Gemini)
- [x] Enriched AI History room (to 10 tasks) with new interactive components (Timeline, Categorize, Scenario, Sorting). (by Gemini)
- [x] Enriched theory content for rooms: `ai-rag` (5 chapters), `ai-security` (5 chapters), `ai-agents` (8 chapters), `native-multimodality` (5 chapters) to meet AGENTS.md depth standards. (by Gemini)
- [x] Expanded task sets to 6 tasks each for `ai-rag`, `ai-security`, `ai-agents`, and `native-multimodality`, incorporating `categorize` and `scenario` task types. (by Gemini)
- [x] Fixed syntax errors and formatting in room data files for multiple rooms. (by Gemini)
- [x] Cleaned up unused imports in multiple theory components to reduce lint warnings. (by Gemini)

## Completed (2026-03-15 session — by Claude Code)
- [x] Built 3 new task components: `TaskCategorize`, `TaskTimeline`, `TaskScenario` (by Claude Code).
- [x] Built `Scenario Mission` task format with brief, constraints, scoring rubric, final decision report (by Claude Code).
- [x] Created room: Fine-Tuning & Adaptation (`fine-tuning-101`) — 12 tasks incl. categorize, scenario, mentor (by Claude Code).
- [x] Enriched fine-tuning-101 theory from detailed MD draft (9 chapters: LoRA math, pipelines, API, decision tree) (by Claude Code).
- [x] Created room: Embeddings & Vector Search (`embeddings-101`) — 10 tasks incl. timeline, categorize, sorting, scenario (by Claude Code).
- [x] Fixed smart quote / Unicode parsing issues across `AiSingularityTheory`, `PromptEvalsTheory`, `rooms.ts` (by Claude Code).
- [x] Removed orphaned duplicate task blocks in `rooms.ts` (after `ai-research` and `prompting-101`) (by Claude Code).
- [x] Synced all Russian mirror docs: PROGRESS.ru.md, CURRICULUM.ru.md, BACKLOG.ru.md, ROOMS_IDEAS.ru.md (by Claude Code).
- [x] Updated ROOMS_IDEAS.md from stale "9 rooms" to actual 19 rooms (by Claude Code).
- [x] Added mandatory agent rules to AGENTS.md: chapter text depth gate, chapter heading typography lock (by Claude Code).

## In Progress
- [ ] Launch DEC-002 implementation track for AI Learn Platform (`dec-002-platform-pipeline`): adapt Agent Ops into a terminal-session autonomous loop with 3 autonomy levels, Telegram approval gates, tiered review loop, quiet hours, and 4-hour digest cadence.

## Future Content (Room Pipeline)
- [x] **Literature Review in the AI Era** (`ai-literature-review`) — shipped 2026-07-31; see the Completed log at the top of this file. Rigorous AI-assisted literature review (query decomposition, source triage, deduplication, PRISMA-style screening, citation verification, synthesis, guarding against hallucinated references), two-way cross-linked with `deep-search-agents`. (requested by owner 2026-07-24)
- [ ] **Room 203: EvalOps Basics** — Build eval sets, score prompts/models, regression checks.
- [ ] **Room 301: AI Product Systems** — Routing, fallback, cost controls, incident playbooks.
- [ ] **AI Ethics & Bias** (`ai-ethics-bias`) — Training data bias, fairness metrics, case studies, responsible deployment.
- [ ] **AI in Production: Cost & Deployment** (`ai-production`) — Token pricing, latency budgets, caching, model routing, monitoring.
- [ ] **Code Generation & AI-Assisted Dev** (`ai-code-generation`) — Copilot/Cursor/Claude Code patterns, code review, test generation.
- [x] **Embeddings & Vector Search** (`embeddings-101`) — Embeddings, semantic similarity, vector DBs, chunking strategies.

## Grand Path Epic: Agent Coding
- [ ] **Grand Task: Agent Coding Path** (`agent-coding`) — launch a full end-to-end path with 12 rooms, practical labs, and capstone delivery.
- [x] **AC-101: Agent Coding Foundations** — problem framing, fast iteration loops, acceptance criteria.
- [x] **AC-102: Agentic Coding Tools** — tool-layer architecture, contracts, safety boundaries, release controls.
- [x] **AC-103: Prompt Contracts for Coding Agents** — translating product intent into prompt specs and failure boundaries.
- [x] **AC-104: Multi-Agent Collaboration Patterns** — decomposition, context windows, branch-per-task discipline.
- [x] **AC-201: CLI Tools for Agent Coding** — terminal discovery/change/verify loop, quality gates, rollback discipline.
- [x] **AC-202: Agentic Testing Loop** — test-first prompts, regression harnesses, flaky-test control.
- [x] **AC-203: Agentic UI Delivery** — design-to-code workflows, responsive constraints, accessibility checks.
- [ ] **AC-301: Shipping Agentic Features with Guardrails** — policy checks, red-team prompts, deployment gates.
- [ ] **AC-302: Cost & Latency Control for Agents** — model routing, caching, budget limits, observability.
- [ ] **AC-303: Team Protocols for Agent Coding** — PR templates, review rubrics, handoff standards.
- [ ] **AC-401: Production Incidents in Agentic Systems** — rollback playbooks, prompt hotfixes, postmortem templates.
- [ ] **AC-402: Agent Coding Capstone** — ship a feature from brief to production-grade release with eval evidence.

## Future Product & Learning
- [ ] Add adaptive learning flow: repeat weak topics + personalized next-room recommendations.
- [ ] Add spaced repetition mode (daily short review from previously failed tasks).
- [ ] Implement a reusable roadmap-style view mode for trajectory, path-selection, and progression screens using the `ROADMAP_VIEW_MODE.md` reference.
- [ ] Add Prompt Playground lab (system prompt, temperature, output comparison).
- [ ] Add Mini RAG lab with user-provided documents.
- [ ] Add motivation loops: weekly challenges, badges, shareable completion certificates.
- [ ] Expand settings: profile management (login/email/password), privacy controls (export/delete data), session controls.
- [ ] Expand settings: notification preferences (daily reminders, weekly summary).
- [ ] Add AI learning preferences: explanation depth and hint mode.
- [ ] Instrument analytics: task drop-off, room/path completion funnels, guest-to-auth conversion.

## Roadmap Additions (March 2026)
- [x] Build `Scenario Mission` task format (brief, constraints, scoring rubric, final decision report).
- [x] Add first mission to `llm-landscape` room (model choice + cost/safety tradeoff).
- [ ] Build Prompt Lab MVP (`/${lang}/labs/prompt-compare`) with side-by-side outputs.
- [ ] Add cost/latency estimator panel to Prompt Lab compare view.
- [ ] Implement "Mistake Notebook": save failed tasks and generate adaptive retries.
- [ ] Add free-text answer task type with rubric-based AI mentor feedback.
- [ ] Add content freshness metadata to rooms (last updated + source links).
- [ ] Add community sharing MVP for mission solutions (publish/bookmark/moderation queue).

## Completed (Backend & Auth)
- [x] Backend persistence (FastAPI + PostgreSQL, user accounts + progress stored server-side).
- [x] User authentication (signup/login/logout, JWT in httpOnly cookies).
- [x] Points system (+10 per task) and streak tracking.
- [x] Frontend auth integration (AuthProvider, useAuth hook, login page).
- [x] Navbar/Sidebar show real user data (points, streak, username, level).
- [x] useProgress hook upgraded to API with localStorage fallback.
- [x] Docker Compose setup for PostgreSQL.
- [x] Alembic migrations for database schema.

## Future Engineering
- [x] Sync guest localStorage progress to server on signup.
- [ ] Room completion analytics / leaderboard.
- [ ] Reduce lint warnings across theory components and hooks.
